# 项目级 AI 编程约束规则

以下规则是在修改本项目中任何代码**之前**必须强制执行的步骤。

## 核心前提：文档先行
在着手编写、修改任何业务代码（前端、后端等）之前，**必须**首先完成以下文档的审阅与相应更新，以保证设计与代码的一致性：

1. **更新 $\textbf{产品需求文档}$ (如 `产品需求文档.md`)**
   - 记录新增或修改的业务需求背景、功能点描述。
   - 记录预期的产品行为、界面交互变更。
   - 若无对应文件，需主动提示并协助创建或在相关的设计文档中补充。

2. **更新 $\textbf{架构设计文档}$ (如 `架构设计文档.md` 或相关系统设计文档)**
   - 记录因本次需求引发的组件拆分、模块新增、依赖关系的变动。
   - 绘制或描述数据流向、核心状态管理策略的变更。

3. **更新 $\textbf{API接口定义文档}$ (如 `openapi.yaml` 或 `API接口文档.md`)**
   - 如果需求涉及到后端接口的新增或字段修改，必须先在 API 文档中定义好入参、出参及 HTTP 状态码。
   - 确保前后端对数据结构的约定已在规范层面达成一致。

## 执行流程要求
- **步骤 1**：接收到新需求后，主动询问并确认相关文档的位置（如果在项目中找不到默认的文档）。
- **步骤 2**：修改或编写相应的需求、架构、API 文档，并与用户确认设计无误。
- **步骤 3**：仅在文档更新完毕并被认可后，才开始相关的代码开发工作。如果是在多次对话中持续进行，必须在每次决定编写相关模块代码前，脑内（Thought 中）或显性核对文档是否已更新。
