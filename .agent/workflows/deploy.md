---
description: 登录服务器并部署服务端代码
---

# 部署服务端代码

服务端由于使用了远程机器，部署流如下：

1. **登录服务器：**
   // turbo
   ```bash
   ssh ubuntu@1.14.45.242
   ```

2. 登录后，**项目目录路径为：** `/projects/jxcServer`。

3. 服务端代码开发完后，需拉取最新代码到该目录并根据当前技术栈重新构建或重启服务。
