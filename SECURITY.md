# 安全策略

## 报告安全问题

请**不要**通过公开 issue、讨论区或 PR 报告安全漏洞。

请通过 GitHub 的私密渠道报告：打开仓库的 **Security** 标签页，选择
**Report a vulnerability** 提交私密安全公告。如果该入口不可用，请通过 GitHub 个人主页上的
联系方式私下联系维护者（[Drac0nids](https://github.com/Drac0nids)）。

报告时请尽量包含：

- 受影响的版本或 commit；
- 部署形态（SaaS / Windows 单机版 / Android 单机版）；
- 复现步骤或 PoC；
- 影响范围评估（例如是否可跨租户读取数据）。

## 处理流程

- 我们会尽快确认报告，并同步处理进展；
- 修复发布前请勿公开细节；
- 修复完成后，如你愿意，会在公告中致谢。

## 安全相关设计说明

以下不是漏洞，但自部署时请留意：

- **默认账号**：单机版首次启动会创建 `admin` / `admin123`，请在正式使用前修改密码。
- **明文传输**：单机版内嵌服务端只监听 `127.0.0.1`；Android 端为此启用了
  `usesCleartextTraffic`。对外提供 SaaS 服务时请务必置于 HTTPS 反向代理之后。
- **密钥配置**：`JWT_SECRET` 等敏感配置通过环境变量注入（见 `server/.env.example`），
  不要提交到仓库。
- **数据库凭据**：`server/.env` 已在 `.gitignore` 中，请勿强行入库。

## 支持范围

安全修复只针对 `main` 分支上的最新代码。
