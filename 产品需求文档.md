# 📄 产品需求文档（PRD）- SaaS 进销存系统

| 文档属性 | 内容 |
| --- | --- |
| 项目名称 | 极速云进销存（Hyper JXC） |
| 文档版本 | v1.1.0（优化版） |
| 文档状态 | 可用于研发实施 |
| 适用范围 | Rust(Axum) 后端、Tauri Windows 端、Flutter Android 端 |

---

## 1. 产品概述

### 1.1 产品定位
面向小微批发/零售商家的多租户 SaaS 进销存系统，提供“商品-库存-采购-销售-报表”闭环管理。

- **Windows 端（Tauri）**：侧重重交互与经营管理（建档、开单、报表、设置、打印）。
- **Android 端（Flutter）**：侧重移动作业（扫码入库、扫码开单、库存盘点、弱网操作）。

### 1.2 目标用户
- 小微商贸企业老板（经营分析、权限配置）
- 采购/库管（收货入库、盘点调整）
- 销售员（销售开单、查价查库存）

### 1.3 MVP 目标（v1.1）
1. 完成采购入库、销售出库、库存盘点三条主链路。
2. 保证库存与成本数据一致、可追溯。
3. 支持多角色协作与租户数据隔离。
4. 支持 Android 弱网下“先作业后同步”。

### 1.4 非目标（本期不做）
- 复杂促销引擎（满减、组合券）
- 多仓调拨高级策略（波次、路径优化）
- 蓝牙打印（App 端，留在后续版本）

---

## 2. 统一术语与口径（跨文档必须一致）

| 术语 | 定义 |
| --- | --- |
| `tenant_id` | 租户ID，所有业务数据必须带租户隔离 |
| `biz_no` | 业务单号（采购单、销售单、盘点单等） |
| `current_stock` | 当前可用库存数量（整型） |
| `unit_cost` | 某次入库的单次进价（请求参数） |
| `cost_price` | 商品当前移动加权平均成本价（商品字段） |
| `request_id` | 每次请求的链路追踪ID（响应返回） |
| `X-Idempotency-Key` | 写接口幂等键，防止重复提交 |

> 约束：金额字段传输使用**字符串**（如 `"12.50"`），后端映射 `rust_decimal`；严禁 `float/double` 参与金额计算。

---

## 3. 角色与权限模型

### 3.1 角色定义
- `OWNER`：租户管理员，拥有全部权限。
- `PURCHASER`：采购/库管，负责商品、采购、库存。
- `SALES`：销售员，负责商品查询、销售开单。

### 3.2 权限矩阵（MVP）

| 模块 | OWNER | PURCHASER | SALES |
| --- | --- | --- | --- |
| 租户/员工管理 | ✅ | ❌ | ❌ |
| 商品管理（增删改查） | ✅ | ✅ | 只读 |
| 采购入库 | ✅ | ✅ | ❌ |
| 销售出库 | ✅ | ❌ | ✅ |
| 库存盘点/调整 | ✅ | ✅ | ❌ |
| 报表查看 | ✅ | ✅（库存类） | ✅（个人销售） |

### 3.3 操作级权限（关键高风险动作）

| 操作 | OWNER | PURCHASER | SALES | 备注 |
| --- | --- | --- | --- | --- |
| 查看 `cost_price` | ✅ | ✅ | ❌ | SALES 返回 `null` |
| 作废采购单 | ✅ | ✅（需授权） | ❌ | 仅允许指定状态作废 |
| 作废销售单 | ✅ | ❌ | ✅（仅本人草稿） | 已确认单据需走反向流程 |
| 导出经营报表 | ✅ | ✅（库存类） | ❌ | 防止敏感利润外泄 |
| 修改库存阈值 | ✅ | ✅ | ❌ | 记录审计日志 |
| 员工权限变更 | ✅ | ❌ | ❌ | 高风险操作 |

---

## 4. 功能需求（Functional Requirements）

## 4.1 认证与租户模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 租户注册 | P0 | 手机/邮箱注册并初始化租户 | 注册成功后生成 `tenant_id` 与 Owner 账号 |
| 用户登录 | P0 | 账号密码登录，返回 Access + Refresh Token | Token 可解析出 `tenant_id/user_id/role` |
| 刷新令牌 | P0 | Access Token 过期后可无感刷新 | Refresh 成功返回新 Access Token |
| 员工密码重置 | P1 | 仅 OWNER 可重置员工密码 | 非 OWNER 操作返回 403 |
| 多设备互踢 | P2 | 同账号可配置单点登录策略 | 开启策略后新登录可使旧会话失效 |

## 4.2 商品管理模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 新建商品 | P0 | 必填：名称、条码、单位；条码租户内唯一 | 重复条码返回冲突错误码 |
| 扫码建档 | P0 | App 扫到未知条码时引导快速建档 | 自动回填条码并可提交建档 |
| 商品列表与搜索 | P0 | 支持分页、名称/条码搜索、分类过滤 | 10万 SKU 下查询响应 < 1s（索引命中） |
| 多价格管理 | P1 | `retail_price/wholesale_price` 可配置，`cost_price` 自动计算 | 前端不可直接修改 `cost_price` |
| 图片管理 | P1 | App拍照/PC上传，关联商品图片URL | 上传后商品详情可展示图片 |

## 4.3 采购入库模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 采购开单 | P0 | 选择供应商、商品、数量、进价，确认入库 | 入库后库存增加且生成入库单据 |
| 扫码入库 | P0 | App 连续扫码自动累加数量，可改进价 | 单次扫码反馈 < 500ms，支持防抖 |
| 成本计算 | P0 | 入库提交后自动重算 `cost_price` | 与移动加权公式一致，保留4位小数 |
| 入库历史 | P1 | 按时间、供应商、商品维度查询 | 可分页、可导出 |

## 4.4 销售出库模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 销售开单 | P0 | 支持搜索/扫码选品，提交出库 | 成功后库存扣减并生成销售单 |
| 防超卖 | P0 | 默认不允许负库存，库存不足拒绝 | 返回明确错误码与失败商品信息 |
| 挂单/草稿 | P1 | 支持临时保存购物车 | 恢复挂单后可继续结算 |
| 小票打印（PC） | P1 | 支持58/80mm热敏小票 | 订单完成后可一键打印 |

## 4.5 库存管理模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 实时库存查询 | P0 | 支持列表/扫码查库存 | 返回 `current_stock` 与预警状态 |
| 库存流水审计 | P0 | 任意库存变化必须写 `stock_logs` | 日志不可修改、不可删除 |
| 库存盘点 | P1 | 生成盘点单->录入实盘->确认调整 | 自动计算盘盈盘亏并写调整流水 |
| 库存预警 | P1 | `current_stock < min_stock_limit` 提醒 | PC/App 首页显示预警数量 |

## 4.6 报表模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 经营看板 | P0 | 今日销售额、毛利、订单数、预警数 | 支持按日查看 |
| 销售报表 | P1 | 按商品/客户/员工统计 | 支持时间范围过滤 |
| 利润分析 | P1 | `销售额 - 销售成本 = 毛利` | 口径与库存成本一致 |

---

## 5. 跨端功能矩阵（MVP）

| 功能模块 | Windows 端 | Android 端 | 说明 |
| --- | --- | --- | --- |
| 商品建档 | ✅ 完整 | ✅ 极简 | App 优先支持现场建档 |
| 采购入库 | ✅ 表单 | ✅ 扫码（重点） | 双端共享同一入库API |
| 销售开单 | ✅ 表单+扫码枪 | ✅ 移动开单 | 同步库存规则一致 |
| 库存盘点 | ⚠️ 基础查询 | ✅ 重点功能 | 盘点场景以移动端为主 |
| 报表分析 | ✅ 全功能 | ✅ 简版看板 | App 只保留核心指标 |
| 小票打印 | ✅ | ❌ | App 蓝牙打印放P2 |

---

## 6. 核心业务规则（必须落地）

### 6.1 移动加权平均成本

```text
new_cost_price = ((old_stock * old_cost_price) + (in_qty * unit_cost)) / (old_stock + in_qty)
```

- 当 `old_stock + in_qty <= 0` 时，`new_cost_price = unit_cost`。
- 计算与存储统一保留 4 位小数。

### 6.2 库存扣减与负库存规则
- 默认不允许负库存（租户可配置是否允许）。
- 不允许负库存时：出库前必须校验 `current_stock >= qty`。

### 6.3 库存流水不可变
- 所有库存变化必须落 `stock_logs`。
- `stock_logs` 仅允许新增，不允许更新和删除。

### 6.4 幂等与离线同步
- 所有写接口需支持 `X-Idempotency-Key`。
- App 弱网离线写入本地队列，网络恢复后重放；服务端按幂等键防重。

### 6.5 幂等细则（必须明确）
1. 幂等键建议使用 UUID v4，作用域：`tenant_id + method + path + key`。
2. 幂等记录最短保留 24 小时（可配置）。
3. 相同幂等键 + 相同请求体：返回首次结果（响应可带 `X-Idempotent-Replay: true`）。
4. 相同幂等键 + 不同请求体：返回冲突错误（建议错误码 `4092`）。

### 6.6 离线冲突处理策略
1. 客户端离线写请求需携带 `expected_version`（商品或单据版本）。
2. 服务端版本不一致时返回 `4091`（版本冲突）并返回最新快照。
3. App 对冲突单据标记为“待处理”，不自动覆盖服务端数据。
4. 用户确认后可选择“刷新后重提”或“放弃本地变更”。

### 6.7 单据状态机与可逆规则

| 单据 | 状态流转 | 规则 |
| --- | --- | --- |
| 采购单 | `DRAFT -> CONFIRMED -> VOIDED` | `CONFIRMED` 后作废必须生成反向库存流水 |
| 销售单 | `DRAFT -> CONFIRMED -> RETURNED_*` | 已确认单据不允许直接删除，只能退货冲销 |
| 盘点单 | `DRAFT -> COUNTING -> CONFIRMED` | `CONFIRMED` 后不可编辑实盘数量 |

> 总原则：禁止“物理删除 + 直接改库存”，必须通过业务动作产生可审计流水。

---

## 7. 非功能需求

### 7.1 性能
- App 扫码识别到商品信息展示：`< 500ms`
- 单租户并发终端：`>= 10`
- 商品查询（10万 SKU）P95：`< 1s`

### 7.2 一致性与可靠性
- 库存相关操作必须事务化，关键行使用 `SELECT ... FOR UPDATE`。
- 关键写路径失败可回滚，禁止半成功状态。

### 7.3 安全
- JWT 鉴权 + Refresh Token 续期。
- 全链路租户隔离与 RBAC 校验。
- 敏感操作写审计日志。

### 7.4 可观测性
- 每个请求输出 `request_id` 并落日志。
- 核心指标：接口延迟、错误率、库存冲突率、同步成功率。

### 7.5 报表口径一致性
- 所有经营日报按租户时区自然日统计（默认 `Asia/Shanghai`）。
- 毛利口径：`实收销售额 - 销售成本`，退货需冲减对应期间数据。
- 报表导出与页面展示使用同一计算口径，避免“页面与导出不一致”。

---

## 8. 里程碑建议

- **M1（2周）**：认证、商品、采购入库、销售出库主链路。
- **M2（2周）**：库存盘点、预警、看板报表、导出能力。
- **M3（1-2周）**：弱网离线同步、幂等完善、性能优化与压测。

---

## 9. 研发对齐要求

1. PRD、架构文档、API 文档三者字段命名必须一致。
2. 响应结构统一为：`code/message/data/request_id`。
3. 库存字段统一为：`current_stock`。
4. 成本字段语义固定：`unit_cost`（入库请求）与 `cost_price`（商品成本）。
