# 📄 产品需求文档（PRD）- SaaS 进销存系统

| 文档属性 | 内容 |
| --- | --- |
| 项目名称 | 极速云进销存（Hyper JXC） |
| 文档版本 | v1.5.0（新增：ADMIN 副管理员角色、OWNER 唯一性约束、员工删除功能） |
| 文档状态 | 可用于研发实施 |
| 适用范围 | Rust(Axum) 后端、Tauri Windows 端、Flutter Android 端 |

---

## 1. 产品概述

### 1.1 产品定位
面向小微批发/零售商家的多租户 SaaS 进销存系统，提供“商品-库存-采购-销售-报表”闭环管理。

- **Windows 端（Tauri）**：侧重重交互与经营管理（建档、开单、报表、设置、打印）。
- **Android 端（Flutter）**：侧重移动作业（扫码入库、扫码开单、库存盘点、弱网操作）。

### 1.2 目标用户
- 小微商贸企业老板（经营分析、权限配置）
- 采购/库管（收货入库、盘点调整）
- 销售员（销售开单、查价查库存）

### 1.3 MVP 目标（v1.1）
1. 完成采购入库、销售出库、库存盘点三条主链路。
2. 保证库存与成本数据一致、可追溯。
3. 支持多角色协作与租户数据隔离。
4. 支持 Android 弱网下“先作业后同步”。

### 1.4 非目标（本期不做）
- 复杂促销引擎（满减、组合券）
- 多仓调拨高级策略（波次、路径优化）
- 蓝牙打印（App 端，留在后续版本）

---

## 2. 统一术语与口径（跨文档必须一致）

| 术语 | 定义 |
| --- | --- |
| `tenant_id` | 租户ID，所有业务数据必须带租户隔离 |
| `biz_no` | 业务单号（采购单、销售单、盘点单、入库/出库作业号等），由系统创建时自动生成 |
| `current_stock` | 当前可用库存数量（整型） |
| `unit_cost` | 某次入库的单次进价（请求参数） |
| `cost_price` | 商品当前移动加权平均成本价（商品字段） |
| `snapshot_sell_price` | 库存流水中的成交单价快照（销售相关流水可为空，历史兼容） |
| `snapshot_inbound_unit_cost` | 库存流水中的“本次入库真实单次进价快照”（仅入库相关流水写入） |
| `request_id` | 每次请求的链路追踪ID（响应返回） |
| `X-Idempotency-Key` | 写接口幂等键，防止重复提交 |

> 约束：金额字段传输使用**字符串**（如 `"12.50"`），后端映射 `rust_decimal`；严禁 `float/double` 参与金额计算。
>
> 补充约束：当服务端发生请求体解析失败（如 JSON 语法错误、字段类型不匹配、Content-Type 不正确）时，仍必须返回统一响应结构 `code/message/data/request_id`，业务码按 `4000` 处理，不允许返回非标准纯文本错误体。

---

## 3. 角色与权限模型

### 3.1 角色定义
- `OWNER`：老板（租户唯一），拥有全部权限，包含人员管理。每个租户只能有一个 OWNER，不可通过修改角色创建新 OWNER。
- `ADMIN`：副管理员，可管理 PURCHASER/SALES 员工（修改角色、重置密码、删除），不能管理 OWNER。
- `PURCHASER`：采购/库管，负责商品、采购、库存。
- `SALES`：销售员，负责商品查询、销售开单。

### 3.1.1 角色层级（rank）

| 角色 | rank | 说明 |
| --- | --- | --- |
| `OWNER` | 100 | 唯一，最高权限 |
| `ADMIN` | 50 | 副管理员 |
| `PURCHASER` | 10 | 普通业务角色 |
| `SALES` | 10 | 普通业务角色 |

> 操作者只能管理 rank 严格低于自己的用户，不能越级操作。

### 3.2 权限矩阵（MVP）

| 模块 | OWNER | ADMIN | PURCHASER | SALES |
| --- | --- | --- | --- | --- |
| 租户/员工管理（增删改查）| ✅ | 可管理 PURCHASER/SALES | ❌ | ❌ |
| 商品管理（增删改查） | ✅ | ✅ | ✅ | 只读 |
| 采购入库 | ✅ | ✅ | ✅ | ❌ |
| 销售出库 | ✅ | ✅ | ❌ | ✅ |
| 库存盘点/调整 | ✅ | ✅ | ✅ | ❌ |
| 报表查看 | ✅ | ✅（全部） | ✅（库存类） | ✅（个人销售） |

### 3.3 操作级权限（关键高风险动作）

| 操作 | OWNER | ADMIN | PURCHASER | SALES | 备注 |
| --- | --- | --- | --- | --- | --- |
| 查看 `cost_price` | ✅ | ✅ | ✅ | ❌ | SALES 返回 `null` |
| 作废采购单 | ✅ | ✅ | ✅（需授权） | ❌ | 仅允许指定状态作废 |
| 作废销售单 | ✅ | ✅ | ❌ | ✅（仅本人草稿） | 已确认单据需走反向流程 |
| 导出经营报表 | ✅ | ✅ | ✅（库存类） | ❌ | 防止敏感利润外泄 |
| 修改库存阈值 | ✅ | ✅ | ✅ | ❌ | 记录审计日志 |
| 员工权限变更 | ✅（全部） | ✅（仅 PURCHASER/SALES） | ❌ | ❌ | 高风险操作，遵循层级约束 |
| 员工密码重置 | ✅（全部） | ✅（仅 PURCHASER/SALES） | ❌ | ❌ | 非授权操作返回 403 |
| 删除员工 | ✅（全部） | ✅（仅 PURCHASER/SALES） | ❌ | ❌ | 不能删除自己 |
| 创建 ADMIN | ✅ | ❌ | ❌ | ❌ | 仅 OWNER 可创建副管理员 |
| 创建 OWNER | ❌ | ❌ | ❌ | ❌ | OWNER 由注册时系统生成，唯一 |

---


## 4.2 商品管理模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 新建商品 | P0 | 必填：名称、条码、单位、`retail_price`、`cost_price`（页面文案：进货价格）；条码租户内唯一 | 重复条码返回冲突错误码 |
| 条码建档（创建表单） | P0 | App 端通过“创建商品”表单建档，支持摄像头扫码回填条码 | 条码可扫码回填并成功提交建档 |
| 商品列表与搜索 | P0 | 支持分页、名称/条码搜索、分类过滤 | 10万 SKU 下查询响应 < 1s（索引命中） |
| Android 商品管理 | P0 | App 端支持商品列表/搜索/条码筛选/编辑/删除（按角色） | OWNER/PURCHASER 可写，SALES 只读且隐藏成本价 |
| 多价格管理 | P1 | 商品创建主作业字段为 `retail_price/cost_price`（文案：进货价格）；后续采购入库按移动加权自动更新 `cost_price`，并维护商品级“最近入库进货价”用于下次默认值 | 价格口径与后端成本计算一致 |

### 4.2.3 新建商品表单字段收敛（本次新增）

1. 新建商品页面将“成本价”文案统一为“进货价格”。
2. 新建商品页面去掉“批量销售价”输入项。
3. “进货价格”输入位放置于原“批量销售价”所在位置，保持一线录入习惯。
4. 字段语义保持：该“进货价格”仍对应商品字段 `cost_price`（移动加权成本初始值）。
| 图片管理 | P1 | App拍照/PC上传，关联商品图片URL | 上传后商品详情可展示图片 |

### 4.2.1 Android 商品管理补充说明（本次新增）

1. **入口与可见性**
   - Android 首页（Dashboard）提供“商品管理”入口。
   - `OWNER/PURCHASER`：可执行新建、编辑、删除。
   - `SALES`：仅可查询与查看，不可写，且不展示 `cost_price`。

2. **核心交互流程**
   - 列表查询：支持 `page/page_size/keyword/barcode`。
   - 条码筛选：在商品列表通过 `barcode` 精确筛选，可使用手机摄像头扫码回填后查询。
   - 编辑保存：提交 `expected_version` 以支持冲突检测。
   - 删除商品：默认携带 `expected_version`，且仅当 `current_stock = 0` 才允许删除。

3. **验收标准（Android）**
   - 能完成“查询 -> 新建 -> 编辑 -> 删除（零库存）”全流程。
   - 写接口请求均附带 `X-Idempotency-Key`。
   - 出现 `4091` 时给出“版本冲突，请刷新后重试”的可执行提示。
   - `SALES` 角色在 App 端看不到成本价，且写按钮不可操作。

4. **交互布局优化（本次新增）**
   - 商品管理默认展示“商品列表页”，聚焦筛选、浏览与操作。
   - 新建商品采用 **FAB（浮动按钮）+ 弹出式表单**（BottomSheet）以缩短高频新增路径。
   - 编辑商品采用 **独立全屏编辑页**（不使用小弹窗），保障长表单可读性与提交稳定性。
   - 商品列表项采用卡片化信息分层（标题、库存/价格摘要、操作区），提升移动端扫读效率。

5. **交互验收补充（Android）**
   - 从列表页点击 FAB，可在不离开列表上下文的情况下完成商品新建。
   - 从列表项进入编辑页，完成保存后返回列表并看到最新数据。
   - 长表单编辑过程中，键盘弹出不遮挡关键提交区，错误提示可见且可理解。

### 4.2.2 新建商品条码自动命名（第三方查询 + 本地缓存，本次新增）

#### 4.2.2.1 目标
1. 在“新建商品”场景下，输入/扫码条码后，系统可自动给出建议商品名称，降低人工录入成本。
2. 通过“本地缓存优先 + 第三方回源”控制外部 API 调用成本与响应时延。
3. 对查无数据条码启用负缓存（Negative Caching），避免短时间内重复回源。

#### 4.2.2.2 业务流程（MVP）
1. 用户在新建商品表单输入或扫码条码。
2. 客户端调用条码查询接口（`GET /products/barcode-lookup?barcode=...`）。
3. 服务端先查本地条码缓存：
   - 命中且未过期：直接返回缓存结果；
   - 未命中或已过期：调用第三方条码 API 回源。
4. 第三方回源结果：
   - 命中商品名称：写入本地缓存（`FOUND`），返回建议名称；
   - 未命中：写入本地负缓存（`NOT_FOUND`）并返回未命中状态。
5. 客户端将建议名称回填到“商品名称”输入框，用户可手工修改后提交创建。

#### 4.2.2.3 交互与权限约束
1. 该能力属于读取能力，`OWNER/PURCHASER/SALES` 均可调用查询接口。
2. 自动回填名称仅作为“建议值”，不锁定输入，允许用户二次编辑。
3. 若第三方服务异常，前端仍允许继续手工录入，不阻断建档主流程。

#### 4.2.2.4 验收标准
1. 当条码在缓存中命中且未过期时，接口返回 `cache_hit=true`，且响应时间显著低于回源路径。
2. 当条码首次查询命中第三方数据时，返回建议名称，并可在后续查询中命中本地缓存。
3. 当条码第三方未命中时，返回 `NOT_FOUND`，并在负缓存有效期内不重复回源。
4. 新建商品页面在“本地商品不存在”场景下可自动带出建议名称，且用户可编辑。
5. 任何场景下统一响应结构保持 `code/message/data/request_id`。

## 4.3 采购入库模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 采购开单 | P0 | 选择供应商、商品、数量，`unit_cost` 选填；不填时按 `last_inbound_unit_cost ?? cost_price` 回落后确认入库 | 入库后库存增加且生成入库单据 |
| 扫码入库 | P0 | 支持两模式：`scan_confirm`（默认）与 `continuous_scan`；支持扫码枪与手机摄像头 | 单次扫码反馈 < 500ms，支持防抖与去重 |
| 成本计算 | P0 | 入库提交后自动重算 `cost_price` | 与移动加权公式一致，保留4位小数 |
| 入库历史 | P1 | 按时间、供应商、商品维度查询 | 可分页、可导出 |

### 4.3.12 入库进货价格默认值持久化（本次新增）

1. 入库页中“单次进价”文案统一改为“进货价格”。
2. 默认值来源规则：
   - 首次默认值取商品录入时的进货价格（`cost_price`）；
   - 若用户在入库时修改过进货价格，下次默认值取该商品“上次入库使用的进货价格”。
3. 为保证 Windows / Android 一致，默认值来源必须由服务端持久化维护，不依赖客户端本地会话态。
4. 该持久化字段仅用于“下次默认值”，不改变移动加权成本与库存流水口径。

### 4.3.1 入库扫码模式补充（本次新增）

1. **模式定义**
   - `scan_confirm`：扫码命中后进入确认区，确认数量/进价后再写入表单（默认模式）。
   - `continuous_scan`：开启连续扫码会话后，摄像头不自动关闭，支持连续识别并累加，直到用户手动结束。

2. **连续扫码会话交互要求**
   - 会话期间每次识别成功后，弹窗顶部实时状态条以绿色展示命中商品名及累加数量（格式：商品名 xN）。
   - 会话期间每次识别失败时，实时状态条以红色展示未命中条码及说明，提示退出后手工处理。
   - 触觉反馈：命中成功使用轻触觉（lightImpact）；未命中使用中等触觉（mediumImpact），手感区分两种结果。
   - 弹窗内维护最近 4 条扫码历史（含图标、商品名或条码、时间戳），方便操作员快速核对。
   - 弹窗标题栏右侧显示本次会话成功入库数量徽章。
   - 会话提供"结束扫码"动作，结束后关闭摄像头并返回页面。
   - 手电筒：扫码弹窗取景框右上角提供手电筒切换按钮，点击可开/关手机闪光灯，在光线不足时使用（前置摄像头或不支持手电筒时自动隐藏）。

3. **风控约束**
   - 连续扫码摄像头检测模式必须使用 `DetectionSpeed.normal`（持续检测），而非 `noDuplicates`（后者在扫到码后会暂停检测直到条码离框，导致用户需要手动触屏才能继续，不符合连续扫码预期）。
   - 同码短时去重由代码层时间窗口（默认 1200ms）负责，与检测模式解耦。
   - 入库在 `unit_cost` 为空时，需提示将回落商品当前 `cost_price`。

4. **撤销最后一条扫码（v1.4.2 新增）**
   - 连续扫码弹窗状态条上，最近一次**成功**扫码右侧显示「↩ 撤销」按钮。
   - 点击后调用 `onUndo(barcode)` 回调：入库页将对应 draft item qty - 1（变 0 则删行）；出库页同理。
   - 撤销成功：已处理计数 -1，状态条及历史行变灰并加删除线显示「已撤销：xxx」。
   - 撤销失败（回调返回 false）：显示 SnackBar 提示「撤销失败，请手动修改」。
   - 只能撤销最后一条成功扫码（不支持任意历史撤销）；`onUndo` 为可选参数，不传时不显示撤销入口（向后兼容）。

### 4.3.2 扫码未命中直达新建商品（本次新增）

1. **触发条件**
   - 在采购入库页执行扫码（扫码枪/摄像头/手输条码）时，调用本地商品查询接口返回 `4040`（条码未建档）。

2. **交互要求**
   - 页面需弹出明确提示：当前条码未建档，可立即新建商品。
   - 提供主动作“新建商品”，次动作“取消/继续扫码”。
   - 点击“新建商品”后，进入商品创建流程并自动回填当前条码。

3. **权限约束**
   - 仅 `OWNER/PURCHASER` 展示“新建商品”入口。
   - 对无建档权限角色，不展示新建入口，仅提示“无建档权限，请联系管理员”。

4. **回流要求**
   - 商品创建成功后，客户端应返回入库上下文（原入库页），并保留条码用于继续入库操作。
   - 创建失败或用户取消时，不中断原扫码入库流程，允许继续手工或扫码作业。

### 4.3.3 扫码模式记忆与确认后自动续扫（本次新增）

1. **扫码模式记忆（入库/出库）**
   - 客户端需记住用户上次选择的扫码模式（`scan_confirm / continuous_scan`）。
   - 记忆范围按“用户 + 终端 + 页面”隔离（入库与出库分别记忆）。
   - 首次无记录时仍使用系统默认模式（入库与出库均默认 `scan_confirm`）。
   - 兼容迁移：若读取到历史模式值 `quick_accumulate`，客户端需自动映射为 `continuous_scan` 后再渲染与持久化。

2. **扫码后确认自动续扫（scan_confirm 增强）**
   - 在 `scan_confirm` 模式下，识别条码后弹出确认表单（数量/价格等），用户确认写入。
   - 确认完成后默认保持在扫码会话中，直接进入下一次扫码等待，不要求再次点击“扫码”按钮。
   - 页面需提供“结束扫码”动作，允许用户主动结束会话。

3. **风控与可用性约束**
   - 连续作业场景下必须保留同码短时去重能力。
   - 会话状态需可见（进行中/未开始、已处理条数）。
   - 本需求仅改变客户端交互与本地偏好存储，不改变后端 API、权限与错误码语义。

### 4.3.4 入库确认写入弹窗化（本次新增）

1. **适用范围**
   - 仅针对入库页 `scan_confirm` 模式。
   - `continuous_scan` 保持原交互，不引入确认弹窗。

2. **交互要求**
   - 扫码命中商品后，展示“确认写入”弹窗（Modal / Dialog），不再占用页面内联确认区。
   - 弹窗需展示命中商品信息（商品ID、名称）与数量输入框（默认 `1`）。
   - 弹窗打开后自动聚焦数量输入框并选中默认值，便于扫码枪/键盘直接改量。

3. **快捷键与续扫要求**
   - `Enter`：确认写入并关闭弹窗。
   - `Esc`：取消本次确认并关闭弹窗。
   - 确认成功后自动回到扫码等待态（聚焦扫码输入框），无需重复点击“按条码处理”。
   - 提交入库成功后，页面应清空商品标识上下文（`product_id/barcode` 与扫码输入），避免误将上一商品继续入库。
   - 提交成功后数量字段恢复默认值 `1`，用于下一笔入库初始录入。

4. **验收标准**
   - `scan_confirm` 模式下每次命中均通过弹窗完成确认。
   - 业务员可仅用“扫码枪 + 回车”完成“扫描 -> 确认 -> 续扫”闭环。
   - 入库提交成功后，表单回到初始态：商品ID/条码为空、数量默认 `1`、不保留上次商品上下文。
   - 不改变后端接口、请求参数、错误码与权限语义。

### 4.3.5 批量入库接口（本次新增）

1. **目标与适用场景**
   - 支持一次提交多条入库明细，降低高频场景下“逐条提交”的交互与网络开销。
   - 适用于 Windows 与 Android 端在“已收集多条入库明细”后的集中提交。

2. **接口要求**
   - 新增接口：`POST /inventory/inbound/batch`。
   - 请求体采用 `items[]` 承载多条入库项，每项延续单条入库字段语义（`product_id/barcode/qty/unit_cost/expected_version/remark`）。
   - 仍要求写接口幂等头：`X-Idempotency-Key`。

3. **事务与一致性要求（关键）**
   - 批量入库必须保证**同请求原子性**：任一明细失败则整批回滚，不允许部分成功。
   - 批量内每个商品仍按移动加权公式更新 `cost_price`，并写不可变 `stock_logs`（`IN_PURCHASE`）。
   - 批量请求成功后返回统一 `biz_no`（整批共享）与每个商品的最新库存快照。

4. **兼容策略**
   - 既有 `POST /inventory/inbound` 保持可用，作为单条兼容路径。
   - 客户端可按场景选择单条或批量接口；本次新增不改变原错误码语义。

5. **验收标准**
   - 同一次批量请求中，若存在任一非法明细（如数量非法、版本冲突、商品不存在），整批不落库。
   - 批量成功后，返回结果可见每个商品 `current_stock/cost_price/version` 最新值。
   - 幂等重放时返回首次结果；同 key 不同请求体返回 `4092`。

6. **客户端落地要求（本次补充）**
   - Windows 与 Android 入库页均需提供“批量明细暂存区”（支持扫码累加、手工增改删）。
   - 当暂存区存在 2 条及以上明细时，提交动作默认调用 `POST /inventory/inbound/batch`。
   - 当暂存区为空或仅单条且用户选择单条提交流程时，可继续调用 `POST /inventory/inbound` 保持兼容。
   - 不得再以“当前已选商品”为由阻断扫码其它商品；跨商品扫码应进入新的明细行或合并到同商品行。

### 4.3.6 入库待提交明细商品名称与进价编辑（本次新增）

1. 入库页“待提交明细”必须展示 `product_name`（商品名称），不允许仅显示 `product_id/barcode`。
2. 待提交明细中的 `unit_cost`（单次进价）必须支持行级编辑，编辑后作用于该行全部数量。
3. 同商品多次扫码合并为同一行时，进价编辑采用“统一修改”策略：
   - 修改的是该商品合并行的当前进价；
   - 提交时按“该行数量 + 该行进价”语义组包。
4. `unit_cost` 仍为可选：
   - 留空时沿用回落规则（服务端回落到商品当前 `cost_price`）；
   - 非空时要求金额格式合法（最多 4 位小数）。
5. 本能力属于客户端编排与展示增强，不新增后端接口，不改变权限、错误码与幂等语义。

### 4.3.7 Android 采购记录入口与只读查询（本次新增）

1. Android 首页（Dashboard）新增“采购记录”快捷入口，入口对 `OWNER/PURCHASER` 可见。
2. 采购记录查询默认参数：
   - `start_date = 当天`
   - `end_date = 当天`
   - `page = 1`
   - `page_size = 10`
3. 采购记录列表支持用户调整每页条数（合法范围 `1~100`），输入为空或非法时客户端回落为 `10`。
4. 采购记录支持日期范围筛选（`YYYY-MM-DD`），开始日期不得晚于结束日期。
5. 采购记录页面仅提供只读能力，不提供采购单修改入口（防篡改）。
6. 列表展示信息需尽可能详细，至少包含：
   - 采购单头：`id/biz_no/status/supplier_id/remark/version/confirmed_at/voided_at/created_at/updated_at`
   - 明细：`product_name/product_id/qty/unit_cost/line_amount`
   - 订单总金额：`total_amount`
7. 验收标准：
   - 首次进入页面自动查询当天采购记录，默认每页 10 条；
   - 可修改日期范围并分页浏览；
   - 列表仅查询展示，不存在编辑/修改入口；
   - 查询失败、日期非法、无权限场景均有可读提示。

### 4.3.8 入库提交按钮位置优化（本次新增）

1. 入库页面交互采用“两段式”动作分层：
   - 表单区主动作仅保留“加入明细”（以及次级“重置”）；
   - “提交入库”主按钮下沉到“待提交入库明细”区域。
2. 提交动作必须与待提交明细同区域，便于用户先核对明细再提交，减少“边录入边提交”误操作。
3. 当待提交明细为空时，“提交入库”按钮应禁用，并给出可理解提示（如“请先加入明细”）。
4. 本优化适用于 Windows 与 Android 双端入库页面，属于客户端交互编排调整：
   - 不新增后端接口；
   - 不改变权限模型、错误码语义与幂等语义。

### 4.3.9 Android 入库记录入口与只读查询（本次新增）

1. Android 首页（Dashboard）将“采购记录”快捷入口调整为“入库记录”，入口对 `OWNER/PURCHASER` 可见。
2. 入库记录查询默认参数：
   - `biz_type = IN_PURCHASE`
   - `start_date = 当天`
   - `end_date = 当天`
   - `page = 1`
   - `page_size = 10`
3. 入库记录列表支持用户调整每页条数（合法范围 `1~100`），输入为空或非法时客户端回落为 `10`。
4. 入库记录支持日期范围筛选（`YYYY-MM-DD`），开始日期不得晚于结束日期。
5. 入库记录页面仅提供只读能力，不提供库存写操作入口（防误操作）。
6. 列表展示信息至少包含：
   - 流水头：`id/biz_type/biz_no/product_id/operator_id/created_at`
   - 变更快照：`delta_qty/snapshot_stock/snapshot_cost`
7. 验收标准：
   - 首次进入页面自动查询当天入库记录（`IN_PURCHASE`），默认每页 10 条；
   - 可修改日期范围并分页浏览；
   - 列表仅查询展示，不存在编辑/修改入口；
   - 查询失败、日期非法、无权限场景均有可读提示。

### 4.3.10 入库记录展示真实进货单价（本次新增）

1. 入库记录列表需展示“本次进货价”，其数据来源为库存流水快照字段 `snapshot_inbound_unit_cost`。
2. 字段语义约束：
   - `snapshot_inbound_unit_cost` 记录本次入库动作的真实单次进价；
   - 当入库请求未传 `unit_cost` 时，按既有回落规则 `effective_unit_cost = unit_cost ?? product.last_inbound_unit_cost ?? product.cost_price` 计算并写入快照。
3. 写入范围约束：
   - `IN_PURCHASE`（含单条入库、批量入库、采购单确认入库）必须写入 `snapshot_inbound_unit_cost`；
   - 非入库类型流水该字段为 `null`（兼容历史与其它业务类型）。
4. 展示约束：
   - Android 入库记录页面在展示“成本快照”同时，增加“本次进货价”；
   - 若字段为空，显示“--”。
5. 兼容性约束：
   - 历史库存流水允许该字段为空；
   - 不改变既有权限模型、错误码语义、幂等语义与统一响应结构。

### 4.3.11 入库页表单收敛与主路径强化（本次新增）

1. 入库页默认主路径为：`扫码 -> 待提交明细 -> 提交入库`。
2. 手工录入表单区默认折叠为“高级录入区”，需要时由用户手动展开。
3. 入库提交动作仅允许从“待提交入库明细”区触发：
   - 至少存在 1 条待提交明细才允许提交；
   - 不再采用“明细为空时回退单表单直提”的页面策略。
4. 手工录入区仍保留“加入明细”能力，用于扫码失败、未建档或异常纠偏场景。
5. 本需求属于客户端交互收敛，不新增后端接口，不改变权限、错误码、幂等与统一响应结构语义。

## 4.4 销售出库模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 销售开单 | P0 | 支持搜索/扫码选品（扫码枪 + 手机摄像头），提交出库；支持 `scan_confirm / continuous_scan` 两模式 | 成功后库存扣减并生成销售单 |
| 防超卖 | P0 | 默认不允许负库存，库存不足拒绝 | 返回明确错误码与失败商品信息 |
| 挂单/草稿 | P1 | 支持临时保存购物车 | 恢复挂单后可继续结算 |
| 小票打印（PC） | P1 | 支持58/80mm热敏小票 | 订单完成后可一键打印 |

### 4.4.1 出库与订单明细交互补充（本次新增）

1. **出库流程模式化**
   - 默认主流程：`扫码后确认`（扫码命中后弹出确认表单，再写入明细）。
   - 可切换会话流程：`连续扫码会话`（打开摄像头后持续扫码，逐条写入/累加，手动结束后关闭摄像头）。
   - 两种模式仅影响前端交互，不改变后端 API 字段与错误码语义。

2. **连续扫码会话反馈**
   - 每次命中后页面顶部显示成功反馈。
   - 每次命中后进行语音播报（默认“扫码成功”）。
   - 会话结束前允许持续扫描下一条码，无需重复开关摄像头。

3. **订单明细编辑策略（防误触）**
   - 订单明细列表默认只读展示。
   - 用户点击“编辑”后，当前行进入可编辑状态；点击“完成”回到只读。
   - 目标：降低误触与误改风险，尤其在高频扫码场景下。

4. **明细初始状态优化**
   - 页面初始为“空明细状态”，不预置默认空首行。
   - 用户可通过“新增明细”或“扫码命中”生成第一条明细。
   - 提交前仍要求至少存在一条有效明细。

### 4.4.2 出库确认写入弹窗化（本次新增）

1. **适用范围**
   - 适用于出库页 `scan_confirm` 模式（Windows + Android）。
   - `continuous_scan` 保持连续会话交互，不引入确认弹窗。

2. **交互要求**
   - 扫码命中后，必须通过“确认写入弹窗”完成明细写入，不再使用页面内联确认区。
   - 弹窗字段至少包含：商品信息、数量、销售单价、可选版本。
   - 弹窗打开后优先聚焦数量输入框，支持键盘/扫码枪快速录入。

3. **快捷键与续扫**
   - `Enter`：确认写入并关闭弹窗。
   - `Esc`：取消本次确认并关闭弹窗。
   - 确认成功后自动回到扫码等待态，延续“确认后续扫”会话。

4. **验收标准**
   - `scan_confirm` 模式下每次命中都通过弹窗确认后写入明细。
   - 不影响后端接口、权限、错误码、幂等语义。
   - 取消确认后，用户可立即继续下一次扫码，不出现状态卡死。
   - Windows 与 Android 双端在 `scan_confirm` 下均采用弹窗，不保留页面内联确认区。

### 4.4.3 出库明细商品名称展示与版本字段收敛（本次新增）

1. 出库页明细区必须展示 `product_name`（商品名称），用于一线核对。
2. 出库明细中的版本字段（`expected_version`）可从默认可见输入项收敛为隐藏/非必填，不作为主作业输入项。
3. 扫码命中生成明细时，需同步写入商品名称；手工新增场景可允许用户补录或按兜底名称展示。
4. 本次属于客户端展示层优化，不改变 `POST /inventory/outbound` 请求结构、权限和错误码语义。

### 4.4.4 出库页表单收敛与高级字段折叠（本次新增）

1. 出库页默认主路径为：`扫码/手工明细 -> 明细核对 -> 提交出库`。
2. 单据头手工录入区（客户、备注、版本）默认折叠，按需展开。
3. 低频字段 `expected_version` 默认放入“高级字段”分组，避免干扰高频作业。
4. 明细编辑、扫码写入、提交校验语义保持不变，不改变后端接口契约。

## 4.5 库存管理模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 实时库存查询 | P0 | 支持列表/扫码查库存（扫码枪 + 手机摄像头） | 返回 `current_stock` 与预警状态 |
| 库存流水审计 | P0 | 任意库存变化必须写 `stock_logs` | 日志不可修改、不可删除 |
| 库存盘点 | P1 | 生成盘点单->录入实盘->确认调整 | 自动计算盘盈盘亏并写调整流水 |
| 库存预警 | P1 | `current_stock < min_stock_limit` 提醒 | PC/App 首页显示预警数量 |

## 4.6 Android 条码录入统一能力（本次新增）

### 4.6.1 目标
- 在 Android App 中，**所有可手动输入条码的输入位**，都必须支持“调用手机摄像头扫码后自动回填”。
- 保留扫码枪/手输方式，摄像头扫码作为并行录入方式，不改变原有 API 契约。

### 4.6.2 覆盖范围（MVP 必须）
1. 入库页
   - 扫码入库条码输入位
   - 入库表单 `barcode` 输入位
2. 出库页
   - 扫码出库条码输入位
3. 盘点页
   - 扫码选品条码输入位
4. 商品页
   - 商品查询筛选 `barcode` 输入位
   - 商品创建 `barcode` 输入位
   - 商品编辑 `barcode` 输入位

### 4.6.3 交互与验收标准
1. 用户点击“手机摄像头扫码”或输入框摄像头图标后，拉起相机扫码面板。
2. 识别到条码后自动关闭扫码面板，并回填到对应输入框。
3. 需要“扫码即执行业务动作”的场景（如扫码入库/出库/盘点选品）应在回填后继续执行原有动作。
4. 相机权限拒绝、无识别结果、取消扫码时，页面不能崩溃，且保留原手动输入流程。
5. 角色权限与接口错误码语义保持不变（`4030/4040/4091` 等）。
6. Android 端条码输入位默认采用“输入框尾部扫码图标”交互，取消独立“手机摄像头扫码”按钮，降低视觉噪音并贴近输入动作。

## 4.7 报表模块

| 功能点 | 优先级 | 需求描述 | 验收标准 |
| --- | --- | --- | --- |
| 经营看板 | P0 | 今日销售额、毛利、订单数、预警数；默认展示当天，可按日历筛选日期 | 支持按日查看 |
| 销售报表 | P1 | 按商品/客户/员工统计 | 支持时间范围过滤 |
| 利润分析 | P1 | `销售额 - 销售成本 = 毛利` | 口径与库存成本一致 |

### 4.7.1 经营看板日期筛选交互补充（本次新增）

1. **默认日期策略**
   - 页面初次进入时，查询日期默认取客户端“当前自然日（YYYY-MM-DD）”。
   - 首屏自动发起当天经营看板查询，降低手工输入成本。

2. **日期选择交互**
   - 查询控件采用日历日期选择器（Date Picker），优先替代纯文本手输。
   - 页面仍以 `YYYY-MM-DD` 口径提交日期参数，保持与后端接口契约一致。

3. **快捷恢复能力**
   - 提供“重置为今天”快捷动作，点击后回填当天日期并自动刷新看板。

4. **兼容与边界**
   - 本次仅为前端交互优化，不新增后端接口，不改变权限与错误码语义。
   - 仍复用 `GET /reports/dashboard?date=YYYY-MM-DD`。

### 4.7.2 经营看板统计口径统一（本次新增）

1. **统计时间口径**
   - `GET /reports/dashboard` 所有“今日”类指标，统一按租户时区自然日统计（当前实现为 `UTC+8`）。

2. **销售额/毛利/热销品口径**
   - 数据源统一使用当日 `stock_logs` 中的销售相关流水：`OUT_SALE`、`RETURN_SALE`。
   - 统计单价采用“快照优先、映射兜底”：
     1) 优先使用库存流水 `snapshot_sell_price`；
     2) 若快照为空（历史数据），再回退 `(biz_no, product_id) -> sales_orders.items.sell_price` 映射。
   - 当“快照与映射都不可用”时，该条流水不计入销售额/毛利/热销统计。

3. **订单数口径（关键）**
   - `total_orders` 统一定义为：当日有效 `OUT_SALE` 流水中，满足“存在有效销售单价（快照或映射）”条件的唯一 `biz_no` 数量。
   - 该口径需覆盖 `SO-*` 与 `OUT-*` 场景（小商家“销售出库即销售”语义）。
   - 不再以 `sales_orders.confirmed_at` 直接计数，避免“已确认但无实际销售流水”造成偏差。

4. **低库存与热销补充规则**
   - `low_stock_count`：当前时点满足 `!is_deleted && current_stock < min_stock_limit` 的商品数。
   - `top_selling_item`：按当日净销量（`OUT_SALE - RETURN_SALE`）最大商品计算；若无正净销量则返回“暂无”。

5. **验收标准**
   - 当存在“已确认但无销售流水”的订单时，不计入 `total_orders`。
   - 当存在“有 `snapshot_sell_price` 但无销售单映射”的 `OUT_SALE/RETURN_SALE` 流水时，仍可计入销售统计。
   - 当存在“快照为空且无法映射”的历史流水时，`total_sales / total_gross_profit / total_orders / top_selling_item` 不受污染。
   - 页面看板与后端接口返回口径一致，可通过集成测试覆盖退货、低库存、热销、`OUT-*` 直出库与历史回退场景。

### 4.7.3 看板订单数下钻与日期范围筛选（本次新增）

1. **下钻入口与可见性**
   - Windows 端经营看板“订单数”指标支持点击下钻。
   - 下钻目标页为“销售单状态流”页面，并自动进入“订单列表筛选区”。
   - 仅 `OWNER/SALES` 允许进入销售单页面并查看下钻列表；`PURCHASER` 仍保留看板指标查看能力但不开放下钻入口。

2. **默认筛选策略**
   - 从看板点击“订单数”跳转时，默认带入看板当前统计日期：
     - `start_date = 当前看板日期`
     - `end_date = 当前看板日期`
   - 从看板点击“订单数”跳转时，客户端应按看板 `total_orders` 动态设置首屏 `page_size`（`1~100`，超出按 `100`），尽可能在首屏展示更多当日订单。
   - 客户端应保证“订单数”点击反馈可感知：
     - 下钻点击热区不应仅限于数字文本，推荐整卡片可点击；
     - 无权限场景点击后需给出明确提示，不允许“静默无反应”。
   - 当订单总数超过单页上限时，仍通过分页查看剩余订单。
   - 销售单页面支持用户修改为任意合法日期范围进行再次筛选。

3. **列表口径一致性（关键）**
   - 下钻订单列表必须与看板 `total_orders` 采用同一统计口径：
     - 仅统计日期范围内、存在有效销售单价（`snapshot_sell_price` 或 `(biz_no, product_id)` 映射）的有效 `OUT_SALE` 流水；
     - 按唯一 `biz_no` 去重后得到订单集合。
   - 若某 `biz_no` 仅存在 `OUT_*` 销售流水、但不存在销售单主记录，仍需在下钻列表中返回“聚合行”（由流水聚合生成）。
   - 禁止出现“看板订单数与下钻列表总数不一致”的情况。

4. **验收标准**
   - 看板点击“订单数”后，销售单页默认展示该日期下钻订单列表。
   - 修改日期范围并查询后，列表可正确刷新且返回总数、分页信息。
   - 在存在“已确认但无有效销售流水”的订单时，不应出现在下钻列表中。
   - 在存在仅 `OUT-*` 流水（无销售单主记录）的场景时，下钻列表应可见该业务单号（聚合行），且 `total` 与看板 `total_orders` 一致。

### 4.7.4 Android 看板“今日销售业务指标”点击反馈与下钻（本次新增）

1. **入口与权限**
   - Android 端经营看板中的“今日销售额”、“今日毛利润”以及“今日订单数”三个指标卡片，统一支持点击下钻。
   - `OWNER/SALES` 点击任一卡片后，均进入当天的“订单下钻列表”页。
   - `PURCHASER` 点击时必须即时无视且提示拦截：`当前角色无销售单下钻权限，仅 OWNER / SALES 可查看`。

2. **默认筛选与分页策略**
   - 首次进入下钻页时，默认带入看板当前统计日期：
     - `start_date = 看板日期`
     - `end_date = 看板日期`
   - 首次进入下钻页时，订单下钻列表默认 `page_size = 10`。
   - 用户可在下钻页手动修改每页条数；当输入为空或非法时，客户端回落为 `10`。
   - 当订单总数超过 `10` 时，通过分页查看剩余数据。

3. **可感知反馈约束**
   - 用户点击后必须有可见反馈（跳转成功、权限提示或失败提示），不得“静默无反应”。
   - 下钻跳转失败或下钻接口查询失败时，必须展示明确错误提示。
   - 日期范围不合法（格式错误、开始晚于结束）时，必须前端即时校验提示。

4. **验收标准**
   - `OWNER/SALES` 点击“今日销售额”、“今日毛利润”或“今日订单数”后，均可进入下钻列表并自动发起首次查询，且默认每页 `10` 条。
   - `PURCHASER` 点击任何一个销售指标卡片时，都能看到明确的权限拦截提示，不出现“点了没反应”。
   - 异常路径（跳转失败、查询失败）有可读错误提示。
   - 本次仅新增 Android 客户端交互能力，不新增后端接口，不改变现有权限与错误码语义。

### 4.7.5 App 日期输入统一日历选择与默认当天（本次新增）

1. Android App 中所有“需要填写日期”的输入位，统一使用日历选择器（Date Picker），不再使用自由文本手工录入日期。
2. 默认值策略：
   - 单日期输入（如经营看板查询日期）默认当天（`YYYY-MM-DD`）；
   - 日期范围输入（如订单下钻 `start_date/end_date`）默认开始、结束都为当天（`YYYY-MM-DD`）。
3. 日历交互要求：
   - 点击日期输入框或日历图标均可拉起日期选择器；
   - 选择后自动回填对应输入框并可立即发起查询。
4. 传参口径不变：客户端仍按既有接口约定提交 `YYYY-MM-DD`，不新增字段。
5. 容错要求：当接口返回空日期或初始化参数缺失时，客户端应回落为当天日期，避免出现空日期输入态。
6. 本需求仅为 Android 客户端交互层优化，不新增后端接口，不改变权限模型、错误码语义与幂等语义。

### 4.7.6 Android 销售趋势与毛利趋势分析页（本次新增）

1. **背景与目标**
   - 老板在首页看到“今日销售额”或“今日毛利润”时，希望点击后能立即看到可视化的经营趋势，而不是纯数字列表。
   - 通过统计图（折线图、柱状图、饼图）增强数据洞察力，帮助经营者快速判断业务走势。

2. **“今日销售额” → 销售趋势分析页**
   - 顶部汇总区：醒目大字展示今日总销售额，副文本“共 N 笔订单”。
   - 图表区 1：**近 7 天销售额折线图**，横轴日期、纵轴金额，点击节点可查看具体数值。
   - 图表区 2：**当日销售额商品构成饼图**（Top 5 商品 + 其它），用于分析收入来源。
   - 底部可展开：当天订单明细列表（复用现有小票化展示）。

3. **“今日毛利润” → 毛利趋势分析页**
   - 顶部汇总区：醒目大字展示今日总毛利，副文本“毛利率 xx%（毛利/销售额）”。
   - 图表区：**近 7 天毛利柱状图**，可叠加销售额对比线，直观看到销售额与毛利的关系。
   - 底部备注：毛利口径说明“毛利 = 销售额 - 销售成本”。

4. **“今日订单数” → 保持现有订单下钻列表页（不变）**

5. **趋势数据来源**
   - 新增后端接口 `GET /reports/trend`，一次返回指定日期范围内的每日汇总数据（销售额、毛利、订单数、低库存数、热销品）。
   - 客户端默认查询近 7 天（`start_date = 今天-6天`, `end_date = 今天`）。

6. **商品构成饼图数据**
   - 复用现有订单下钻接口 `GET /reports/dashboard/orders`，客户端从订单明细中按商品聚合计算销售额占比。
   - 不新增后端聚合接口，保持 MVP 简洁。

7. **权限与交互约束**
   - 仅 `OWNER/SALES` 可进入销售/毛利分析页；`PURCHASER` 点击时弹出权限拦截提示。
   - 图表加载期间展示骨架屏；加载失败时展示明确错误提示。
   - Flutter 端图表库推荐使用 `fl_chart`。

8. **验收标准**
   - 点击“今日销售额”后可看到近 7 天销售趋势折线图 + 当日商品构成饼图。
   - 点击“今日毛利润”后可看到近 7 天毛利柱状图 + 毛利率汇总。
   - 图表具备加载动画、触摸节点可查看数值。
   - 本次需新增后端 `GET /reports/trend` 接口，其余不改变现有权限、错误码与幂等语义。

### 4.7.7 Android 低库存商品下钻页（本次新增）

1. **背景与目标**
   - 首页"低库存商品数"当前仅显示一个数字，缺乏可操作性。
   - 用户需要知道**哪些商品**处于低库存状态，以及每个商品**当前还有多少库存**、**阈值是多少**，方便及时补货。

2. **入口与权限**
   - `OWNER / PURCHASER / SALES`：点击"低库存商品数"后均可进入低库存商品列表页（商品信息属于只读查询，不涉及敏感成本）。
   - 对 `SALES` 角色：隐藏 `cost_price` 字段，保持与商品列表权限一致。

3. **页面内容**
   - 页面顶部展示"当前低库存商品共 N 件"汇总。
   - 列表每项展示：
     - 商品名称（`name`）
     - 条码（`barcode`，可选折叠）
     - 当前库存（`current_stock`）**+ 视觉进度条**（展示库存与阈值关系）
     - 库存阈值（`min_stock_limit`）
     - 差额（`min_stock_limit - current_stock`，即"还差多少才达标"）
   - 支持下拉刷新。

4. **数据来源**
   - 复用现有 `GET /products` 接口，新增可选参数 `low_stock=true`，后端仅返回满足 `current_stock < min_stock_limit` 的商品，并按库存缺口从大到小排序。
   - 不新增独立接口路径，权限与字段结构与现有商品列表完全一致。

5. **约束**
   - 仅为只读展示，不提供"直接补货"跳转（避免功能蔓延，可在 P2 通过按钮跳转采购入库页并预填商品信息）。
   - 不改变现有 `GET /products` 的分页、权限与错误码语义。

6. **验收标准**
   - 点击"低库存商品数"后可见低库存商品列表，每项有商品名 + 当前库存 + 阈值 + 进度条。
   - `SALES` 角色点击可正常进入，但 `cost_price` 不可见。
   - 列表为空时显示"当前没有低库存商品，库存充足 🎉"提示。

## 4.8 Android 移动端 UI/UX 优化（本次新增）

### 4.8.1 目标
1. 提升 Android App 的视觉一致性与专业感，降低“页面简陋、信息堆叠”的主观感受。
2. 按作业频次重排关键入口，优先支持“开单/扫码/提交”等高频动作。
3. 降低单页认知负担：将“筛选、录入、结果反馈”分区展示，减少误操作。

### 4.8.2 交互布局要求（MVP）
1. 首页（Dashboard）
   - 需包含“经营指标区 + 高频作业入口区 + 查询筛选区”三段式结构。
   - 高频入口默认顺序：`销售出库 -> 采购入库 -> 库存盘点 -> 商品管理`。
2. 认证页（登录/注册）
   - 统一为单卡片表单布局，字段按“必填优先”顺序展示。
   - 需提供明确状态反馈（提交中、失败原因、成功后跳转）。
3. 作业页（入库/出库/盘点/商品）
   - 扫码区与表单区需明显分组，避免混淆。
   - 错误提示必须靠近当前操作区域，并保持可读文案。

### 4.8.3 视觉规范要求（MVP）
1. 全局采用统一主题（颜色、圆角、间距、按钮层级、输入框样式）。
2. 关键按钮（提交/查询/扫码）使用主按钮样式，次级动作使用弱化按钮样式。
3. 信息卡片采用统一卡片样式，保证页面层级清晰、可扫读。

### 4.8.4 盘点页引导步进式（Stepper）体验重构（本次新增）
1. 痛点：原盘点三个步骤（创建->查询/开始->确认实盘）为平铺卡片布局，信息堆积导致视觉疲劳，且状态感知弱。
2. 交互重构要求：
   - 引入引导步进式（Stepper）折叠布局，明确区分为：**步骤1（创建） -> 步骤2（查询与流转） -> 步骤3（录入实盘）**。
   - 当完成其中一步（如创建成功、或查询到 DRAFT 状态单据）后，自动折叠前一步并平滑展开、滚动至下一步。
   - 保留头部品牌大图（BrandHeroBanner）与尾部最后一次盘点结果卡片常驻，保证全局操作上下文可见。
3. 约束：此改造仅为前端 UI 组件树及布局流转优化，不改变背后的数据流转、后端接口参数以及原有错误码逻辑。

### 4.8.5 验收标准（Android）
1. 用户可在 3 秒内识别首页主要操作入口。
2. 常用作业路径（扫码 -> 填充 -> 提交）交互连贯，不出现跳读。
3. 登录页、看板页、作业页视觉风格统一，不出现明显“拼装感”。
4. 盘点页流转具备清晰向导感（步骤完成后能自动展开下一步）。
5. 本轮优化不改变既有 API 契约、权限模型与错误码语义。

## 4.9 面向小微商户的业务单号自动化（本次新增）

### 4.9.1 目标
1. 降低一线人员录入负担：创建入库/出库/采购单/销售单/盘点单时不再要求手填业务单号。
2. 降低误填风险：业务单号由系统生成并作为系统追踪字段，创建后不可人工修改。
3. 保持可追溯性：所有库存流水、单据详情、报表关联继续使用 `biz_no`。

### 4.9.2 范围（MVP）
1. 以下创建动作统一改为“服务端自动生成 `biz_no`”：
   - 采购入库 `/inventory/inbound`
   - 销售出库 `/inventory/outbound`
   - 创建采购单 `/purchase-orders`
   - 创建销售单 `/sales-orders`
   - 创建盘点单 `/inventory/stock-checks`
2. Android 创建页不再展示“可编辑业务单号输入框”，创建成功后在结果区展示系统生成单号。
3. Windows（Tauri）创建页同样不再展示“可编辑业务单号输入框”，创建成功后在结果区展示系统生成单号。

### 4.9.3 验收标准
1. 用户在创建流程中无需输入 `biz_no` 仍可成功提交。
2. 响应 `data` 中返回系统生成的 `biz_no`，并可用于后续查询与审计。
3. 库存流水中的 `biz_no` 与对应单据保持一致。
4. 幂等机制与错误码语义保持不变（`X-Idempotency-Key`、`4091/4092` 等）。
5. Windows 与 Android 双端创建交互保持一致：创建前不要求输入 `biz_no`，创建后均可查看服务端回传 `biz_no`。

### 4.9.4 兼容策略
1. 老版本客户端若继续传入 `biz_no`，服务端不作为创建主依据（系统以自动生成值为准）。
2. 后续若需记录外部纸质单号，新增独立字段（如 `external_ref_no`）承载，不复用 `biz_no`。

### 4.9.5 Windows 端创建交互约束（本次新增）
1. 入库、出库、采购单、销售单、盘点单的创建表单中，不再提供业务单号手工输入控件。
2. Windows 端创建请求体不再主动提交 `biz_no` 字段。
3. 创建成功后，Windows 页面结果区需展示响应 `data.biz_no`，用于打印/核对/追溯。
4. 本次仅调整前端交互与请求组包，不改变服务端权限、幂等与错误码语义。

## 4.10 Android 高配软装 UI 体验升级（本次新增）

### 4.10.1 目标
1. 将 Android 端界面从“功能可用”提升为“品牌化、精致化、可持续扩展”的视觉体验。
2. 在不改变既有业务语义与 API 契约前提下，统一视觉语言与页面骨架。
3. 提升关键路径（登录、看板、入库、出库、盘点、商品管理）的可读性与操作愉悦感。

### 4.10.2 范围（MVP）
1. 全局主题升级：
   - 支持 Light / Dark 双主题。
   - 统一颜色语义（主色、成功、警告、错误、信息）、圆角、间距、阴影、动效时长。
2. 页面骨架升级：
   - 统一采用“品牌头图区 + 业务分区卡片 + 状态反馈区”的结构。
3. 通用组件升级：
   - 新增品牌头图组件、分区容器组件、统一状态提示组件。
4. 关键页面升级：
   - 登录页、经营看板、商品管理、采购入库、销售出库、库存盘点。

### 4.10.3 交互与视觉要求
1. 主动作（提交/查询/扫码）必须具备明显视觉优先级，次动作弱化。
2. 页面状态（成功/失败/警告/只读）须统一样式与位置，避免文案散落。
3. 高频信息与关键指标需分层展示，确保 3 秒内可定位核心操作入口。
4. 动效采用轻量微动效策略，不影响业务操作效率。

### 4.10.4 验收标准
1. 登录页与核心业务页视觉风格统一，无明显“拼装感”。
2. 看板与作业页可识别分区结构（品牌头图、操作区、结果区）。
3. 亮色/暗色主题下文本可读性达标（不出现大面积低对比）。
4. 本轮仅进行 UI/UX 升级，不变更后端接口路径、字段、权限语义与错误码语义。

## 4.11 扫码文案与行为一致性规范（本次新增）

### 4.11.1 目标
1. 消除“按钮写着扫码，但实际未拉起摄像头”的认知偏差。
2. 统一 Windows 与 Android 双端在条码相关按钮上的命名语义。
3. 在不改动既有 API 与业务流程的前提下，降低一线操作误解与误触。

### 4.11.2 命名规则
1. **仅在会直接拉起手机摄像头时**，按钮/tooltip 才允许使用“扫码…”动词。
   - 示例：`摄像头扫码`、`扫码填充条码`。
2. 对“处理当前输入框条码（含扫码枪输入）”的动作，统一使用非摄像头语义文案。
   - 示例：`按条码处理`、`按条码加入明细`、`按条码查询`、`处理当前条码并累加`。
3. “连续扫码会话”属于模式名称，可保留；但会话内手动触发处理按钮仍按第 2 条命名。

### 4.11.3 验收标准
1. Windows 与 Android 端入库/出库/盘点/商品查询页面，不再出现“扫码并xx”但实际仅处理输入框条码的主按钮文案。
2. 摄像头图标按钮/入口文案明确体现“摄像头”语义。
3. 本次仅调整前端文案，不新增接口，不改变错误码、权限与提交参数。

## 4.12 扫码模式切换交互优化（本次新增）

### 4.12.1 目标
1. 降低扫码模式切换成本：将“隐藏在下拉框中的模式选择”改为“可见可点的并排切换控件”。
2. 保持“动作文案”和“实际行为”一致，避免误导。
3. 在 Android 端增强单手效率：当用户点击主处理按钮且条码为空时，允许智能拉起摄像头扫码。

### 4.12.2 交互规则
1. 入库/出库页的扫码模式选择（`scan_confirm / continuous_scan`）统一改为并排 Tab（或分段按钮），不再使用下拉框。
2. 模式切换仅改变前端交互状态机，不新增后端接口与参数。
3. 主按钮继续使用“按条码处理/按条码加入明细”语义，不与“摄像头扫码”语义混用。
4. Android 端主按钮支持智能触发：
   - 当前模式为 `scan_confirm` 且条码输入为空时，点击主按钮可直接拉起摄像头；
   - 识别成功后继续执行原模式处理流程；
   - 取消扫码或识别失败时，不中断原手工输入流程。
5. `continuous_scan` 模式保持“显式进入会话”行为，不受第 4 条影响。

### 4.12.3 验收标准
1. Windows 与 Android 入库/出库页可直接看到两种模式并一键切换。
2. Android 入库/出库页在非连续模式下，条码为空时点击主按钮可拉起摄像头并完成后续处理。
3. 全链路保持原有接口、错误码、权限与幂等语义不变。

## 4.13 订单小票化展示增强（本次新增）

### 4.13.1 目标
1. 让采购单、销售单展示形态更接近超市小票，降低一线人员核对成本。
2. 在订单明细中同时展示“商品名称 + 数量 + 单价 + 行小计”，并在订单级展示总金额。
3. 统一前后端金额口径，避免客户端重复计算带来的展示分叉。

### 4.13.2 范围（MVP）
1. **销售单响应增强**
   - `items[*]` 新增：
     - `product_name`
     - `line_amount`（`qty * sell_price`，金额字符串，4 位小数）
   - 订单级 `total_amount` 保持返回。
2. **采购单响应增强**
   - `items[*]` 新增：
     - `product_name`
     - `line_amount`（`qty * unit_cost`，金额字符串，4 位小数）
   - 订单级新增 `total_amount`（所有行小计求和，金额字符串，4 位小数）。
3. **Windows 端订单页面展示增强**
   - 采购单、销售单结果区明细表增加“商品名称、行小计”。
   - 采购单结果区补充展示“订单总金额”。

### 4.13.3 验收标准
1. 销售单与采购单详情接口返回中，明细均可见 `product_name` 与 `line_amount`。
2. 采购单详情接口返回中可见 `total_amount`，且等于所有 `line_amount` 之和（四舍五入到 4 位小数）。
3. Windows 销售单与采购单页面可直接展示“商品名称/数量/单价/行小计/总金额”核心信息。
4. 本次改造不新增接口路径，不改变权限模型、错误码语义与幂等语义。

### 4.13.4 订单明细商品名称快照固化（本次新增）

1. 采购单明细、销售单明细在创建时，服务端需将当时商品名称固化到明细快照字段（内部存储字段）。
2. 订单详情响应中的 `product_name` 需优先读取明细名称快照，避免商品后续改名导致历史订单展示失真。
3. 对历史旧单（尚无名称快照）保持兼容：
   - 优先回退商品主数据映射名称；
   - 仍缺失时回退 `商品#<product_id>`。
4. 该能力属于“口径固化”增强：不新增接口路径，不增加请求参数，不改变权限、错误码和幂等语义。
5. 验收口径：完成订单后再修改商品名称，新创建订单按新名称展示，历史订单继续保持创建时名称（对已具备快照的数据）。

### 4.13.5 订单列表小票化展示（本次新增）

1. 销售单页面中的“订单下钻列表”也需采用小票化展示，不再仅展示订单级摘要列。
   - 该约束同时适用于 Windows 与 Android 两端的订单下钻页面。
2. 每笔订单在列表中至少展示：
   - 订单头：`biz_no`、状态、订单总金额；
   - 明细行：商品名称、数量、销售单价、行小计；
   - 订单尾：创建时间、更新时间、备注。
3. 对 `OUTBOUND_ONLY` 等聚合行，后端应优先返回可聚合的明细（商品名称、数量、单价）；客户端仍须保留订单级展示（业务单号、总金额、备注）。当历史数据确实无法聚合时，允许 `items=[]` 并显示“无明细（聚合行）”。
4. 本次为展示层增强：不新增接口路径，不改变筛选分页参数、权限模型、错误码语义与幂等语义。
5. 验收标准：在同一订单列表中可直接完成“按单核对总金额 + 按明细核对行小计”，无需再进入单据详情页。

### 4.13.6 Android 订单下钻字段精简展示（本次新增）

1. Android 端“订单下钻列表”在每笔订单明细中，仅展示以下字段：
   - `product_name`（商品名称）
   - `qty`（商品数量）
   - `sell_price`（商品单价）
2. Android 端“订单下钻列表”不再展示明细行 `line_amount`（行小计）。
3. Android 端“订单下钻列表”订单尾时间字段仅展示 `created_at`（创建时间），不再展示 `updated_at`（更新时间）。
4. 对 `OUTBOUND_ONLY` 等聚合行，若后端返回聚合明细则按普通明细渲染；仅在无可聚合明细时才显示 `items=[]` 占位提示，并保留订单头、创建时间、备注。
5. 本次为 Android 客户端展示层字段收敛，不新增后端接口，不改变接口返回字段、权限模型、错误码语义与幂等语义。

### 4.13.7 订单下钻聚合行明细回填（本次新增）

1. 对来源于库存流水且无 `sales_orders` 主记录的 `OUTBOUND_ONLY` 聚合单，服务端需按 `(biz_no, product_id, sell_price)` 聚合生成 `items` 明细。
2. 聚合明细至少包含：`product_name`、`qty`、`sell_price`（并继续兼容返回 `line_amount`、`returned_qty`）。
3. 商品名称读取规则：优先商品主数据映射名称，缺失时兜底 `商品#<product_id>`。
4. 仅当历史流水缺失关键字段导致无法聚合时，才允许返回 `items=[]`。
5. 验收口径：订单下钻列表在 `OUTBOUND_ONLY` 场景下，默认可见“每种商品名称 / 单价 / 数量”。

## 4.14 Windows 端视觉体系升级（本次新增）

### 4.14.1 配色系统升级
1. 主品牌色由“默认蓝”升级为更克制的深夜蓝体系，避免旧式企业软件观感。
2. 页面背景采用浅灰/米白层次，卡片可使用低饱和品牌浅底（约 5% 品牌色透明度）提升呼吸感。
3. 功能色语义固定：
   - 入库：翡翠绿
   - 出库：琥珀橙
   - 库存不足：珊瑚红

### 4.14.2 卡片与空间体系
1. 卡片圆角统一提升至 `12px~16px`。
2. 移除硬边框主视觉，采用浅弥散阴影表达层级（如 `0 8px 24px rgba(149,157,165,0.1)`）。
3. 统一增加卡片内边距与模块间距，强调留白，提升重点数据可读性。

### 4.14.3 文本与数据层级
1. 经营看板“今日销售额”需加粗放大，数字采用等宽数字字体提升对齐观感。
2. 次要信息（如 ID、单号、操作人）下沉为中灰小号文本。
3. 数据可视化增强：
   - 库存数量后展示微型进度条（可视化当前库存与阈值关系）；
   - 今日销售额展示旁提供微型趋势线（Sparkline）。

### 4.14.4 图标体系
1. 首页入口与导航入口采用双色（Duotone）面性图标风格，提升识别度。
2. 列表操作（编辑/删除）采用统一线性图标（线宽、圆角一致）。

### 4.14.5 动效与加载反馈
1. 数据加载时优先使用骨架屏（Skeleton），替代单一“加载中”旋转语义。
2. 经营看板金额支持进入页面后的数字滚动动画（0 -> 目标值）。
3. 页面切换（如进入扫码入库）增加平滑转场（淡入/轻微位移或缩放），避免生硬跳转。

### 4.14.6 验收标准
1. Windows 端主要页面（看板、商品、入库、出库、低库存）视觉风格统一且层级清晰。
2. 看板可见金额滚动动画与微型趋势线；低库存页可见库存进度条。
3. 列表操作按钮使用线性图标，导航入口体现双色图标风格。
4. 本次仅改前端展示与交互体验，不改变后端接口、权限与错误码语义。

---

## 5. 跨端功能矩阵（MVP）

| 功能模块 | Windows 端 | Android 端 | 说明 |
| --- | --- | --- | --- |
| 商品建档 | ✅ 完整 | ✅ 管理版（列表/搜索/扫码/建档/编辑/删除） | App 侧支持现场建档与移动维护 |
| 采购入库 | ✅ 表单 | ✅ 扫码（重点） | 双端共享同一入库API |
| 销售开单 | ✅ 表单+扫码枪 | ✅ 移动开单 | 同步库存规则一致 |
| 库存盘点 | ⚠️ 基础查询 | ✅ 重点功能 | 盘点场景以移动端为主 |
| 报表分析 | ✅ 全功能 | ✅ 简版看板 | App 只保留核心指标 |
| 小票打印 | ✅ | ❌ | App 蓝牙打印放P2 |

---

## 6. 核心业务规则（必须落地）

### 6.1 移动加权平均成本

```text
new_cost_price = ((old_stock * old_cost_price) + (in_qty * effective_unit_cost)) / (old_stock + in_qty)
```

- 当 `old_stock + in_qty <= 0` 时，`new_cost_price = effective_unit_cost`。
- 计算与存储统一保留 4 位小数。

### 6.2 库存扣减与负库存规则
- 默认不允许负库存（租户可配置是否允许）。
- 不允许负库存时：出库前必须校验 `current_stock >= qty`。

### 6.3 库存流水不可变
- 所有库存变化必须落 `stock_logs`。
- `stock_logs` 仅允许新增，不允许更新和删除。

### 6.4 幂等与离线同步
- 所有写接口需支持 `X-Idempotency-Key`。
- App 弱网离线写入本地队列，网络恢复后重放；服务端按幂等键防重。

### 6.5 幂等细则（必须明确）
1. 幂等键建议使用 UUID v4，作用域：`tenant_id + method + path + key`。
2. 幂等记录最短保留 24 小时（可配置）。
3. 相同幂等键 + 相同请求体：返回首次结果（响应可带 `X-Idempotent-Replay: true`）。
4. 相同幂等键 + 不同请求体：返回冲突错误（建议错误码 `4092`）。

### 6.6 离线冲突处理策略
1. 客户端离线写请求需携带 `expected_version`（商品或单据版本）。
2. 服务端版本不一致时返回 `4091`（版本冲突）并返回最新快照。
3. App 对冲突单据标记为“待处理”，不自动覆盖服务端数据。
4. 用户确认后可选择“刷新后重提”或“放弃本地变更”。

### 6.7 单据状态机与可逆规则

| 单据 | 状态流转 | 规则 |
| --- | --- | --- |
| 采购单 | `DRAFT -> CONFIRMED -> VOIDED` | `CONFIRMED` 后作废必须生成反向库存流水 |
| 销售单 | `DRAFT -> CONFIRMED -> RETURNED_*` | 已确认单据不允许直接删除，只能退货冲销 |
| 盘点单 | `DRAFT -> COUNTING -> CONFIRMED` | `CONFIRMED` 后不可编辑实盘数量 |

> 总原则：禁止“物理删除 + 直接改库存”，必须通过业务动作产生可审计流水。

---

## 7. 非功能需求

### 7.1 性能
- App 扫码识别到商品信息展示：`< 500ms`
- 单租户并发终端：`>= 10`
- 商品查询（10万 SKU）P95：`< 1s`

### 7.2 一致性与可靠性
- 库存相关操作必须事务化，关键行使用 `SELECT ... FOR UPDATE`。
- 关键写路径失败可回滚，禁止半成功状态。

### 7.3 安全
- JWT 鉴权 + Refresh Token 续期。
- 全链路租户隔离与 RBAC 校验。
- 敏感操作写审计日志。

### 7.4 可观测性
- 每个请求输出 `request_id` 并落日志。
- 核心指标：接口延迟、错误率、库存冲突率、同步成功率。

### 7.5 报表口径一致性
- 所有经营日报按租户时区自然日统计（默认 `Asia/Shanghai`）。
- 毛利口径：`实收销售额 - 销售成本`，退货需冲减对应期间数据。
- 报表导出与页面展示使用同一计算口径，避免“页面与导出不一致”。

---

## 8. 里程碑建议

- **M1（2周）**：认证、商品、采购入库、销售出库主链路。
- **M2（2周）**：库存盘点、预警、看板报表、导出能力。
- **M3（1-2周）**：弱网离线同步、幂等完善、性能优化与压测。

---

## 9. 研发对齐要求

1. PRD、架构文档、API 文档三者字段命名必须一致。
2. 响应结构统一为：`code/message/data/request_id`。
3. 库存字段统一为：`current_stock`。
4. 成本字段语义固定：`unit_cost`（入库请求）与 `cost_price`（商品成本）。
5. 商品创建接口 `cost_price` 必填；采购入库接口 `unit_cost` 选填且缺省回落商品当前 `cost_price`。
6. 商品维度需维护“最近入库进货价”（用于入库默认值），其更新口径与入库有效进价一致：`effective_unit_cost = unit_cost ?? product.last_inbound_unit_cost ?? product.cost_price`。

---

## 10. UI/UX 优化记录

### 10.1 商品管理页重构（v1.3.1）

**变更范围：** `features/products/presentation/products_page.dart`（纯展示层，无 API 变动）

| 分类 | 具体变更 |
|---|---|
| 库存进度条 | 修正公式为 `value = (stock / minStockLimit).clamp(0,1)`；三段色：红<30%/橙<60%/绿≥60% |
| 低库存标识 | 库存胶囊改为「库存 N / 需 M」；追加「差 X 件」红色小标 |
| 价格展示 | 零售价加 ¥ 前缀 + 蓝色大字加粗；进货价并排展示；实时计算毛利率徽章 |
| 分页控件 | 新增居中页码「第 X / Y 页」 |
| 筛选面板 | 可通过 AppBar 图标收起/展开 |
| 关键字搜索 | 420ms 防抖自动触发 + 回车触发 |
| 加载状态 | 骨架屏替代 CircularProgressIndicator |
| 空状态 | 区分「无数据」与「筛选无结果」，附「清除筛选」按钮 |
| 删除确认 | 按钮改为 error 红色 |
| 只读用户 | 商品卡片 tap 进入详情查看 |
| 新建表单 | 分三组：基本信息 / 价格设定 / 库存设置 |

### 10.2 商品卡片价格展示细化（v1.3.2）

将「进货价格」字段拆分为两个独立指标：
- **均摊成本**：加权平均库存成本（cost_price），毛利率计算基准
- **最近进货**：最近一次采购入库单价（last_inbound_unit_cost）

毛利率公式：`(零售价 - 均摊成本) / 零售价 * 100%`

### 10.3 商品创建时自动写入最近进货价（v1.3.3）

**变更范围：** `server/src/routes/products.rs`（后端 create_product handler）

**修复说明：**
创建商品时若 `init_stock > 0`，自动将 `cost_price` 写入 `last_inbound_unit_cost`，
使「最近进货」字段在首次创建后即有数据。
若 `init_stock = 0`，`last_inbound_unit_cost` 保持 NULL，直至首次正式采购入库后更新。

**历史数据补填：** 对已有 `current_stock > 0` 且 `last_inbound_unit_cost IS NULL` 的商品，
执行 `UPDATE products SET last_inbound_unit_cost = cost_price WHERE ...` 补填初始值。

---

## 11. 盘点历史流水页 UI/UX 全面重构（v1.4.0）

### 11.1 后端字段扩展

`GET /inventory/logs` 响应 `StockLogData` 新增：
- `product_name`（String?）：商品名称，从 products 表映射，无则 null
- `operator_name`（String?）：操作人姓名，从 users 表映射，无则 null

### 11.2 前端重构清单

| 分类 | 具体变更 |
|---|---|
| 主标题 | 改为「盘盈 +5」/「盘亏 -3」大号数字，视觉优先传达核心信息 |
| 商品展示 | 优先显示 product_name，无则 fallback 到「ID: xxx」|
| 操作人展示 | 优先显示 operator_name，无则截短 UUID 后8位 + 省略号 |
| biz_type 映射 | 英文 → 中文，不同类型不同颜色徽章 |
| 库存对比 | 「账面库存 100 → 盘后实际 97」水平 Before→After 箭头格式 |
| 时间格式 | ISO 原始串 → 「yyyy/MM/dd HH:mm」本地时间 |
| 日期快捷 | 新增「今日/本周/本月/近30天」ActionChip 快捷选择 |
| 每页条数 | TextField 改为 SegmentedButton（10/20/50）|
| 加载状态 | 骨架屏替代 CircularProgressIndicator |
| 空状态 | 图标 + 文字 + 提示，区分「无数据」情况 |
| 分页控件 | 新增居中「第 X / Y 页」胶囊 |
| 汇总栏 | 独立 SummaryBar 展示「日期区间 共 N 条 第 X/Y 页」 |
| Hero banner | 颜色改为紫色（0xFF8B5CF6）匹配盘点场景 |
| 下拉刷新 | 新增 RefreshIndicator 支持 |
| AppBar 刷新 | 新增右上角刷新按钮 |

---

## 12. 通用筛选组件抽取 + 多页面统一改造（v1.4.1）

### 12.1 新增共用组件（brand_ui.dart）

| 组件 | 说明 |
|---|---|
| `QuickDateChip` | 单个快捷日期 ActionChip |
| `QuickDateChipsRow` | 快捷 Chip 行：今日/本周/本月/近30天 |
| `DatePickerField` | 日期选择按钮，点击弹出 DatePicker |
| `PageSizeSegmented` | SegmentedButton 每页条数选择（10/20/50）|
| `PaginationBar` | 分页导航栏，含「第 X/Y 页」居中胶囊 |

### 12.2 改造范围

| 页面 | 改造内容 |
|---|---|
| `inbound_logs_page` | 复用全套共用组件：快捷日期、DatePickerField、PageSizeSegmented、PaginationBar；改进卡片布局（商品名/操作人姓名/成本 MetaChip）；骨架屏、空状态、RefreshIndicator |
| `dashboard_orders_page` | 替换 TextField → 快捷日期 + DatePickerField + PageSizeSegmented；删除重复 `_PaginationBar` 类 |
| `stock_check_logs_page` | 已在 v1.4.0 完成，作为参考实现 |

### 12.3 已无需改造

| 页面 | 原因 |
|---|---|
| `profit_trend_page` | 固定展示近7天，无用户可操作的日期筛选 |
| `sales_trend_page` | 同上 |

---

## 13. 首页（经营看板）全面优化（v1.5.0）

### 13.1 P1 体验问题修复

| 问题 | 修复 |
|---|---|
| Banner 内有重复刷新按钮 | 删除，只保留 AppBar 右上角刷新按钮 |
| 日期选择器占整个 SectionCard | 改为内联 `_DateSelectorBar`：今日/昨日快捷 Chip + OutlinedButton 日期选择 |
| 权限不足的快捷入口置灰显示 | 改为**完全不渲染**，仅展示当前角色有权限的入口 |
| 权限说明 Chip 暴露内部模型 | 移除，无需对用户展示 |
| 退出登录在 AppBar 右侧易误触 | 移至 `PopupMenuButton` 三点菜单 |
| 用户卡片显示原始租户 ID | 改为「头像 + 姓名 + 中文角色名」只展示有用信息 |

### 13.2 P2 信息展示提升

| 优化 | 效果 |
|---|---|
| 指标卡颜色统一 | 销售额=绿、毛利润=蓝、订单=橙、低库存=红/绿、热销=橙红，每卡独立 accent 色 |
| 低库存预警色 | lowStockCount > 0 时整张卡数值变红色，= 0 时显示绿色 |
| Sparkline 颜色与指标卡一致 | `_SparklinePainter` 接收 accentColor，不再全部用主题主色 |
| AppBar 标题带查询日期 | 「经营看板 · 今天」/「经营看板 · 3月14日」|

### 13.3 P3 细节打磨

| 优化 | 效果 |
|---|---|
| 加载骨架屏改为 5 张 | 与实际指标卡数量一致 |
| 快捷入口 subtitle maxLines: 2 | 防止长文字溢出 |
| UserRole.chineseLabel | 新增所有角色的中文标签映射 |

### 13.4 补充优化：指标卡日期前缀动态化（v1.5.1）

**变更背景：** 用户切换查询日期后，「今日销售额」文案语义错误。

**规则：**
- 查询日期 = 今天 → 显示「**今日**销售额 / 毛利润 / 订单数」
- 查询日期 = 昨天 → 显示「**昨日**销售额 / 毛利润 / 订单数」
- 查询日期 = 其他 → 显示「**M月D日**销售额 / 毛利润 / 订单数」

**低库存商品**和**热销商品**卡片不带日期前缀（它们是当前实时状态，与查询日期无关）。

---

## 14. 库存盘点页历史记录入口优化（v1.5.2）

**问题：** AppBar 右上角仅有一个 `icons.history` 图标按钮，用户容易忽视。

**修复：**
1. **AppBar 入口：** `IconButton` → `TextButton.icon`（图标 + 「盘点历史」文字），视觉权重提升。
2. **页面内快捷卡：** Hero Banner 下方新增 `_HistoryShortcutCard`（紫色 accent，圆角卡片，含标题、说明文字、右箭头），与盘点流程 Stepper 明确区分，用户首屏即可发现入口。

---

## 15. 采购入库页入库记录入口优化（v1.5.3）

**背景：** 入库作业完成后，用户往往立即想查看记录，但需要退出到首页才能找到入口。

**修复（与库存盘点页策略一致）：**
1. **AppBar：** `IconButton(history)` → `TextButton.icon(receipt, '入库记录')`
2. **页面内快捷卡（绿色 accent）：** Hero Banner 下方显示「入库记录查询」卡片，提示日期筛选功能，点击跳转到 InboundLogsPage。

### 15.1 首页入库记录快捷入口删除（v1.5.4）

**背景：** 入库记录已整合至采购入库页内（AppBar + 快捷卡双入口），首页快捷入口造成重复、分散导航结构。

**改动：**
- 删除首页「入库记录」GridView 快捷入口
- 清理 `_QuickActionType.inboundLogs` enum case 及其 switch handler（dead code）
- 移除不再需要的 `import inbound_logs_page.dart`（仍保留 controller 注入，供 InboundPage 传递使用）

---

## 16. 人员管理模块（v1.6.0）

### 16.1 功能概述

新增完整的「人员管理」模块，仅 OWNER 角色可见和操作。

### 16.2 API（后端原有，补充前端实现）

| 接口 | 方法 | 权限 |
|---|---|---|
| `GET /users` | 列出租户下所有用户 | OWNER |
| `POST /users` | 创建新用户 | OWNER |
| `PATCH /users/:id/role` | 修改用户角色 | OWNER |
| `POST /users/:id/reset-password` | 重置用户密码 | OWNER |

### 16.3 前端新增文件

| 文件 | 说明 |
|---|---|
| `users/models/users_models.dart` | UserData、CreateUserRequest、UpdateUserRoleRequest、ResetPasswordRequest |
| `users/models/users_repository.dart` | 包装四个 /users API |
| `users/application/users_controller.dart` | load/createUser/updateRole/resetPassword |
| `users/presentation/users_page.dart` | 完整 UI：列表、角色说明、添加/改角色/重置密码弹窗 |

### 16.4 新增公共基础

- `ApiClient.patch<T>()` — 新增 PATCH HTTP 方法支持

### 16.5 首页入口

首页快捷 Grid 新增「人员管理」卡片（紫色，`manage_accounts` 图标），仅 OWNER 可见。

### 16.6 安全修复：禁止管理员修改自身角色（v1.6.1）

**风险描述：** 系统管理员（OWNER）如果能修改自己的角色为采购员/销售员，将永久失去管理权限，无法恢复。

**修复内容（双层防护）：**

| 层次 | 修复方式 |
|---|---|
| **后端** | `PATCH /users/:id/role` 新增检查：`auth.user_id == user_id` 时返回 403（"不能修改自己的角色"） |
| **前端** | 人员列表中，当前登录用户的行：① 隐藏「修改角色」菜单项 ② 姓名旁显示蓝色「你」标记 |

**当前用户 ID 来源：** `sessionController.session.user.id`，通过 `currentUserId` 参数传入 `UsersPage`。

### 16.7 用户名一键复制（v1.6.2）

**背景：** 老板在人员管理页创建员工后，需要把账号信息告知员工，手动读取用户名容易出错。

**实现：** 用户卡片中 `@username` 文本改为可点击区域：
- 点击后自动将用户名复制到系统剪贴板
- 弹出 SnackBar 确认：「已复制用户名：zhangsan」（2秒后消失）
- 用户名旁显示小复制图标（`copy_rounded`，透明度降低作为视觉提示）

---

## 17. 租户码多租户隔离（v1.7.0）

### 17.1 背景

SaaS 多租户模式下，允许不同公司存在同名员工（如都叫 zhangsan），需通过租户码隔离。

### 17.2 数据库变更（Migration 0009）

| 变更 | 说明 |
|---|---|
| 新建 `tenants` 表 | id UUID, code VARCHAR(8) UNIQUE, name |
| 为老数据回填 code | 取各租户 UUID 第10-15位转大写 |
| 演示租户固定 code | `DEMO01` |
| 改用户名唯一索引 | `ux_users_username` → `ux_users_tenant_username (tenant_id, username)` |

### 17.3 后端接口变更

**POST /auth/register**
- 自动生成 6 位大写字母+数字租户码（字符集排除易混淆字符 O/I/0/1）
- 新建 tenants 记录后再建用户
- 响应新增 `tenant_code` 字段

**POST /auth/login**
- 新增可选字段 `tenant_code`
- 有租户码时：`JOIN tenants WHERE code = ? AND username = ?` 精确匹配
- 无租户码时：向后兼容，按 username 全局查（适用演示/内存模式）
- 响应新增 `tenant_code` 字段（原样返回客户端传入的值）

### 17.4 前端变更

| 文件 | 变更 |
|---|---|
| `login_request.dart` | 新增可选 `tenantCode` 字段 |
| `user_session.dart` | 新增 `tenantCode` 字段，fromJson/toStorageJson 中持久化 |
| `session_storage.dart` | 新增 `readTenantCode`/`writeTenantCode`，key: `jxc.last_tenant_code` |
| `session_controller.dart` | login 接收 `tenantCode` 参数，登录成功后自动保存 |
| `auth_page.dart` | 登录表单新增「租户码」输入框；initState 时从存储自动读取预填 |
| `app.dart` | AuthPage 增加 sessionStorage 参数传递 |

### 17.5 用户体验

```
首次登录：
  租户码输入框 [空/自动预填]  用户名 [xxx]  密码 [***]
  → 登录成功后保存租户码到 SharedPreferences

下次打开 App：
  租户码输入框 [ABC123] ← 自动填好  用户名 [xxx]  密码 [***]
  → 直接点登录即可
```

### 17.6 修复：补注册缺失的 Migration（v1.7.1）

**问题：** `persistence.rs` 的 `apply_postgres_migrations` 只包含到 migration 0007，0008 和 0009 从未被注册，导致服务器启动时这两个迁移不会自动执行。

**修复：** 将以下两个 migration 补充到数组中：
- `0008_products_last_inbound_unit_cost_drop_wholesale_price`
- `0009_tenants_tenant_code_login`

下次 `/deploy` 部署后，服务器启动时将自动执行 0009，完成 tenants 表创建和用户名唯一索引变更。

### 17.7 强制租户码登录 + Migration 幂等性修复（v1.7.2）

**变更1 — 强制要求租户码：**
- 后端：PostgreSQL 模式下 `POST /auth/login` 现在强制要求 `tenant_code` 非空，否则返回 400 "请输入租户码"
- 前端：`_submitLogin` 优先校验租户码非空，弹出提示
- 演示账号提示更新为：`租户码 DEMO01 / admin / admin123`

**变更2 — Migration 幂等性修复：**
- 问题根因：`apply_postgres_migrations` 每次启动都无条件重新执行所有 SQL，已有数据库会因 DDL 冲突（如已删除的 `wholesale_price` 列被重新 ADD）而崩溃
- 修复方案：每个 migration 执行前先查询 `schema_migrations` 表，若 `version` 已存在则跳过
- 效果：新库完整执行所有迁移，旧库只执行新增的迁移（如 0009）

## 18. 商品管理集成盘点快捷入口（v1.8.0）

### 18.1 需求背景

库存盘点与商品管理是两个独立模块，但用户在发现某商品库存异常时，需要在两个页面之间来回切换。通过在商品卡片上加入「发起盘点」快捷按钮，用户可以直接从商品管理页跳转到盘点页，并自动预填商品ID。

### 18.2 交互流程

```
商品管理 → 商品卡片 → [发起盘点] → 库存盘点页（第一行商品ID已预填）
```

### 18.3 变更说明

| 文件 | 变更 |
|---|---|
| `products_page.dart` | `ProductsPage` 新增可选 `onStockCheck` 回调；`_ProductCard` 新增「发起盘点」按钮（仅 canWrite 时显示，有回调时才显示按钮）|
| `stock_check_page.dart` | `StockCheckPage` 新增可选 `initialProductId` 参数，从商品页跳入时自动预填第一行 |
| `dashboard_page.dart` | 导航到商品管理时传入 `onStockCheck` 回调，点击后 push 盘点页并传入 `initialProductId` |

### 18.4 设计决定

- **不合并功能**：两个模块业务逻辑、状态机和代码量差异均较大，合并会导致单文件超过 3000 行
- **通过回调解耦**：`ProductsPage` 不直接依赖 `StockCheckController`，通过 `onStockCheck(productId)` 回调解耦，可在任何地方复用
- **预填但不锁定**：盘点创建表单中预填商品ID，用户仍可修改或追加其他商品

## 19. 库存盘点页面重设计（v1.9.0）

### 19.1 问题诊断

旧版「库存盘点状态流」页面存在以下问题：
- 标题"状态流"等技术性词汇，普通用户难以理解
- `expected_version` 字段直接暴露给用户，用户不知道填什么
- 步骤2「查询单据/开始盘点」职责混乱
- 盘点结果原始展示，像 debug 输出
- 步骤之间需要手动切换，没有自动推进

### 19.2 重设计原则

1. **用业务语言**：去掉"状态流"/"expected_version"等技术词
2. **自动推进**：创建成功→自动跳步骤2；开始成功→自动跳步骤3；查询后根据状态自动定位
3. **隐藏技术细节**：`expected_version` 自动使用 controller 建议值，用户不感知
4. **彩色差异展示**：亏库红色、盈库蓝色、无差异绿色

### 19.3 三步向导

| 步骤 | 标题 | 操作 |
|---|---|---|
| 第一步 | 选择要盘点的商品 | 扫条码/手工添加商品ID，创建盘点单 |
| 第二步 | 开始盘点 | 查询已有单据或直接开始盘点 |
| 第三步 | 录入实盘数量 | 每行预填账面库存，修改后提交 |

### 19.4 结果展示

盘点完成后显示彩色差异看板：
- 🟢 无差异：绿色背景
- 🔴 亏库（实盘 < 账面）：红色，负数差值
- 🔵 盈库（实盘 > 账面）：蓝色，正数差值

## 20. 库存盘点显示商品名称（v1.9.1）

### 20.1 问题

盘点页面所有商品均以 `商品 #ID` 形式展示，用户无法识别具体商品。

### 20.2 方案

**后端变更（`common.rs`、`stock_checks.rs`）：**
- `StockCheckItemData` 新增 `product_name: String` 字段
- `to_stock_check_data` 改为接受 `&HashMap<i64, String>` 参数，在序列化时注入商品名称
- 新增 `to_stock_check_data_with_names` 异步助手，自动调用 `load_product_name_map` 后调用上函数
- 四个路由（create/get/start/confirm）均改为返回含商品名称的数据
- 内存模式下（create handler）同步构建商品名称 map，避免 MutexGuard 跨越 await 点（Send 问题）

**前端变更（`inventory_models.dart`、`stock_check_page.dart`）：**
- `StockCheckItemData` 新增 `productName` 字段，从 `product_name` JSON 字段解析
- 盘点结果面板（`_ConfirmedResultPanel`）直接展示 `item.productName`
- 录入实盘行（`_ConfirmItemRow`）新增 `productName` 参数，从 `result.items` 中按 `productId` 查找并传入

---

## 21. 商品批次管理（v1.4.0）

### 21.1 背景与目标

针对有效期管理需求的商户（食品、日化、医药等），需要追踪每批次商品的入库日期、生产日期与过期日期，并在批次临期时主动预警，避免过期商品流出。

### 21.2 批次数据模型

**`product_batches` 表字段**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | 整数 | 自动 | 主键 |
| `tenant_id` | UUID | 自动 | 租户隔离 |
| `product_id` | 整数 | ✅ | 关联商品 |
| `lot_number` | 字符串 | 可选 | 批次号（留空自动生成 YYYYMMDD-NN，同天同商品第 N 批次，防重复） |
| `supplier` | 字符串 | 可选 | 供应商名称（v1.4.2 新增；可从已有供应商中选择，也可手填临时供应商名称） |
| `inbound_at` | 日期 | ✅ | 入库日期 |
| `produced_at` | 日期 | 可选 | 生产日期 |
| `expires_at` | 日期 | 可选 | 过期日期 |
| `is_sold_out` | 布尔 | 自动 | 是否已售完（手动标记） |
| `notes` | 字符串 | 可选 | 备注 |
| `created_at` | 时间戳 | 自动 | 创建时间 |

**`products` 表新增字段（v1.4.1）**

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `track_batches` | 布尔 | `false` | 是否启用批次追踪；开启后每次入库自动弹批次关联弹窗 |

### 21.3 效期预警规则

有过期日期且 `is_sold_out = false` 的批次按如下规则分级：

| 等级 | 条件 | 颜色 | 说明 |
|---|---|---|---|
| `expired` | `expires_at < 今天` | 灰色 | 已过期 |
| `critical` | 距今 < 7 天 | 红色 | 紧急 |
| `warning` | 距今 < 15 天 | 橙色 | 警告 |
| `notice` | 距今 < 30 天 | 黄色 | 关注 |
| `ok` | 距今 ≥ 30 天 | 默认 | 安全 |

### 21.4 功能需求

1. **批次 CRUD**
   - 创建批次（可在采购入库时关联，也可在批次管理页独立创建）
   - 查看批次列表（按商品筛选，可过滤"仅未售完"）
   - 编辑批次（批次号、日期、备注）
   - **标记售完**（软删除语义，不再参与预警计算）
   - 删除批次

2. **商品维度批次追踪开关**
   - 新建/编辑商品时，提供"启用批次追踪"开关（默认关闭）
   - 开启后：每次该商品成功入库，自动弹出"批次关联"弹窗（详见 §21.7）
   - 关闭后：入库成功后静默，不弹任何批次提示

3. **入库时批次关联**（详见 §21.7）
   - 仅对 `track_batches = true` 的商品触发
   - 入库成功后，按商品维度汇总弹出批次弹窗（一商品一弹窗）
   - 弹窗提供：选择已有批次 / 新建批次 / 跳过 三个选项

4. **效期预警看板**
   - 首页（Dashboard）常驻"批次效期监控"卡片：
     - **有临期批次**：按严重程度排序显示临期批次列表，每行展示商品名、批次号、剩余天数，以及「查看」跳转按钮
     - **无临期批次**：绿色"✅ 暂无临期批次 / 近 30 天内所有批次均安全"状态
   - 预警范围：过期日期在 30 天以内（含已过期）的未售完批次
   - **注意**：首页预警行不提供「标记售完」快捷入口（防止误触），须进入批次管理页操作

5. **批次管理入口**
   - 商品管理页每个商品卡片底部新增"批次"操作按钮，直达该商品的批次管理页
   - 批次管理页显示批次卡片列表，支持编辑、标记售完、删除

### 21.5 权限约束

| 操作 | OWNER | PURCHASER | SALES |
|---|---|---|---|
| 查看批次列表 | ✅ | ✅ | ✅ |
| 创建/编辑批次 | ✅ | ✅ | ❌ |
| 标记售完 | ✅ | ✅ | ❌ |
| 删除批次 | ✅ | ✅ | ❌ |

### 21.6 验收标准

1. 可在商品卡片点击"批次"进入批次管理页，查看该商品所有批次。
2. 能创建批次并填写批次号/日期/备注，支持跳过过期日期。
3. 可手动将批次标记为"已售完"，标记后不再出现在效期预警中。
4. Dashboard 常驻"批次效期监控"卡片，空状态显示绿色安全提示。
5. 有 30 天内临期批次时，卡片显示红/橙/黄色预警信息。
6. 商品新建/编辑页可看到"启用批次追踪"开关，切换后保存生效。
7. 对已开启批次追踪的商品完成入库后，弹出批次关联弹窗（详见 §21.7 验收）。
8. 未开启批次追踪的商品入库后，不弹任何批次提示。

---

## 22. 供应商管理（v1.4.2 新增）

### 22.1 概述

为规范企业采购来源、减少重复录入和命名混乱，系统新增供应商管理模块。用户可维护一份供应商名单，在新建批次时通过 Autocomplete 下拉快速选择，同时保留直接手填临时供应商名称的灵活性。

### 22.2 数据模型

**`suppliers` 表**

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGSERIAL | 主键 |
| `tenant_id` | UUID | 租户隔离 |
| `name` | VARCHAR(128) | 供应商名称（不可为空） |
| `phone` | VARCHAR(32) | 联系电话（可选） |
| `notes` | TEXT | 备注（可选） |
| `is_deleted` | BOOLEAN | 软删除标记 |
| `created_at` | TIMESTAMPTZ | 创建时间 |
| `updated_at` | TIMESTAMPTZ | 更新时间 |

> `purchase_orders.supplier_id` 已通过外键关联 `suppliers.id`（`ON DELETE SET NULL`）。

### 22.3 API 端点

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/suppliers` | ALL | 列表（`?q=关键字` 模糊搜索） |
| POST | `/suppliers` | OWNER / PURCHASER | 新建供应商 |
| PUT | `/suppliers/:id` | OWNER / PURCHASER | 更新供应商 |
| DELETE | `/suppliers/:id` | OWNER | 软删除 |

### 22.4 功能需求

1. **供应商列表页**（从商品管理页 AppBar「供应商」按钮进入）
   - 展示当前租户所有未删除的供应商（名称 + 电话）
   - 支持下拉刷新
   - 右上角「+」新增供应商
   - 每条记录可通过弹出菜单执行「编辑」或「删除」

2. **新建 / 编辑供应商**（底部弹窗）
   - 必填：名称
   - 可选：联系电话、备注
   - 保存时调用对应 API；失败则展示 SnackBar 错误

3. **批次表单集成**（详见 §21.7.3）
   - 供应商字段改为 Autocomplete 组件
   - 聚焦 / 输入时自动请求 `GET /suppliers?q=xxx` 展示匹配结果
   - 选中后回填名称到表单字段
   - 用户可无视下拉直接输入任意名称（兼容临时供应商场景）

### 22.5 权限约束

| 操作 | OWNER | PURCHASER | SALES |
|---|---|---|---|
| 查看供应商列表 | ✅ | ✅ | ✅ |
| 新建 / 编辑供应商 | ✅ | ✅ | ❌ |
| 删除供应商 | ✅ | ❌ | ❌ |

### 22.6 验收标准

1. 商品管理页 AppBar 显示「供应商」按钮，点击跳转供应商列表页。
2. 可在供应商列表页新增、编辑、删除供应商，操作后列表即时更新。
3. 新建批次表单中，供应商字段点击后展示已有供应商下拉建议，输入关键字后过滤。
4. 选中已有供应商后，名称自动回填；也可忽略下拉直接手填。
5. 批次保存后，`product_batches.supplier` 存为选择或输入的名称字符串。

---

## 21.7 入库批次关联弹窗（v1.4.1 新增）

### 21.7.1 触发条件

- 入库（单条 `POST /inventory/inbound` 或批量 `POST /inventory/inbound/batch`）提交**成功**后；
- 服务端响应中，至少有一个商品的 `track_batches = true`；
- 由客户端根据此标记，按商品维度逐个弹出批次关联弹窗。

### 21.7.2 弹窗触发逻辑

1. 入库成功后，客户端从响应中筛选 `track_batches = true` 的商品列表。
2. 对每个此类商品（已按 `product_id` 去重，同一商品多次扫码合并为一条）依次弹出批次弹窗。
3. 弹窗为**模态底部弹窗（ModalBottomSheet）**，不可通过点击背景关闭，必须选择操作。
4. 提供三个操作选项：

| 操作 | 行为 |
|---|---|
| **选择已有批次** | 展示该商品的批次列表（仅显示 `is_sold_out = false` 的批次），用户选择一条后关联本次入库 |
| **新建批次** | 展开新建批次表单（批次号/入库日期/生产日期/过期日期/备注），填写后提交创建新批次 |
| **跳过** | 关闭弹窗，不关联任何批次，继续处理下一个商品 |

### 21.7.3 弹窗字段说明

**选择已有批次**：
- 展示该商品的批次卡片列表，每张卡片显示：批次号（若有）、入库日期、过期日期、效期等级色标
- 支持下拉刷新批次列表
- 选中后展示确认信息，点击"关联"完成

**新建批次**：
- `lot_number`：批次号（可选，最多 64 字符）；留空则由服务端自动生成 `YYYYMMDD-NN`（NN 为当天该商品的第 N 批，从 01 递增），防止同天重复；表单下方 helperText 始终可见，提示用户生成规则
- `supplier`：供应商名称（可选，最多 128 字符；v1.4.2 新增）
  - 表单中为 **Autocomplete 下拉+手填**：聚焦后展示租户已有供应商列表，输入则模糊过滤；也可无视下拉直接手填临时名称
  - 无需强制选择已有供应商，方便临时对接新供应商
- `inbound_at`：入库日期（默认今天）
- `produced_at`：生产日期（可选）
- `expires_at`：过期日期（可选，但建议填写以启用效期预警）
- `notes`：备注（可选）

### 21.7.4 多商品场景

- 批量入库中有 N 个 `track_batches = true` 的商品，则依次弹 N 个弹窗
- 弹窗标题显示当前商品名称，帮助用户识别正在操作哪个商品
- 跳过某个商品不影响后续商品的弹窗触发

### 21.7.5 与已有批次关联的语义

选择已有批次时，本次入库**不修改**原批次的日期信息，仅在业务上表示"本次入库属于该批次"（当前版本记录日志，不新建关联表，仅作为备忘）。

> **后续可扩展**：future 版本可建 `inbound_batch_links` 表追踪每次入库与批次的关联关系，本版本不实现。

### 21.7.6 权限约束

| 操作 | OWNER | PURCHASER | SALES |
|---|---|---|---|
| 触发批次弹窗（入库后） | ✅ | ✅ | ❌（SALES 不可入库） |
| 选择已有批次 | ✅ | ✅ | — |
| 新建批次 | ✅ | ✅ | — |
| 跳过 | ✅ | ✅ | — |

### 21.7.7 验收标准

1. 对 `track_batches = true` 的商品完成入库后，弹出批次关联弹窗。
2. 弹窗标题显示商品名称。
3. "选择已有批次" Tab 中正确展示该商品所有未售完批次，选择后可关联。
4. "新建批次" Tab 中填写信息后可成功创建批次。
5. "跳过"可关闭弹窗，不影响库存数据。
6. 批量入库多个追踪商品时，依次逐个弹窗。
7. 未开启批次追踪（`track_batches = false`）的商品入库后不弹窗。

---

## 22. Android UI 交互优化（v1.4.0）

### 22.1 新建商品按钮位置优化

将"新建商品"从悬浮操作按钮（FAB）迁移至AppBar右侧图标按钮。

**变更前**：浮动按钮（FAB）置于页面右下角，会遮挡列表底部的分页控件。

**变更后**：`+` 图标按钮嵌入 AppBar 右上角操作区，与刷新/分类等操作并排。

- 仅 `OWNER/PURCHASER` 可见该按钮（权限沿用原 canWrite 逻辑）
- 点击行为与原 FAB 一致，打开"新建商品"底部弹窗

### 22.2 刷新按钮移除

移除各列表页的独立刷新 `IconButton`，统一改由**下拉刷新（Pull-to-Refresh）**手势触发。

| 页面 | 变更 |
|---|---|
| 商品管理 | 删除刷新按钮，保留下拉刷新 |
| 批次管理 | 删除刷新按钮，保留下拉刷新 |
| 低库存商品 | 删除刷新按钮，保留下拉刷新 |
| 入库记录 | 删除刷新按钮，保留下拉刷新 |
| 盘点历史流水 | 删除刷新按钮，保留下拉刷新 |
| 人员管理 | 删除刷新按钮，新建员工入口移至 AppBar 右侧图标 |
| 仪表盘 | 保留刷新按钮（实时经营数据） |

### 22.3 日历中文化

通过引入 `flutter_localizations` 并在 `MaterialApp` 设置 `locale: const Locale('zh', 'CN')` 与 `GlobalMaterialLocalizations`，全局将日期选择器（`showDatePicker`）、对话框按钮（确定/取消）等 Material 组件统一国际化为中文。

**影响范围**：
- 批次创建/编辑日期选择器
- 仪表盘日期选择器
- 其他所有使用 `showDatePicker` 的页面

**不改变**：接口参数仍以 `YYYY-MM-DD` 字符串传输，无 API 变更。

### 22.4 验收标准

1. 商品管理页 AppBar 右侧可见 `+` 图标，点击可新建商品。
2. 商品列表滚动到底部，分页控件不被任何悬浮按钮遮挡。
3. 列表页下拉可触发刷新。
4. 所有日历弹窗显示中文（月份/星期/操作按钮）。
5. 人员管理页 AppBar 右侧显示"添加员工"图标，下拉可刷新。

### 22.5 首页智能自动刷新（局部刷新策略）

#### 22.5.1 目标

在不引入 WebSocket / Server-Sent Events 的前提下，通过以下两种轻量机制保证首页数据的时效性：

1. **生命周期刷新**：App 从后台切回前台时，自动重新拉取首页看板与批次预警数据。
2. **操作返回刷新**：从可能改变数据的业务页面（入库/出库/盘点/商品管理）返回首页时，自动触发对应刷新。

#### 22.5.2 刷新触发规则

| 触发场景 | 刷新内容 | 刷新策略 |
|---|---|---|
| App 切回前台（`AppLifecycleState.resumed`） | 看板 + 批次预警 | 防抖（30 秒内不重复） |
| 采购入库页返回首页 | 看板全量刷新 | 强制（必然改变库存/成本） |
| 销售出库页返回首页 | 看板全量刷新 | 强制（改变销售额/订单数） |
| 库存盘点页返回首页 | 看板全量刷新 | 防抖（避免频繁进出反复请求） |
| 商品管理页返回首页 | 仅刷新批次预警卡片 | 立即（可能新建/修改批次） |
| 低库存页 → 入库返回首页 | 看板全量刷新 | 强制 |
| 低库存页直接返回首页 | 看板全量刷新 | 防抖 |
| 人员管理页返回首页 | 不刷新 | — |

#### 22.5.3 防抖规则

- 两次自动刷新间隔 < 30 秒时跳过（`_autoRefresh` 路径）。
- 强制刷新（`_load` 路径）不受防抖限制，用于"确定有数据变化"的场景。

#### 22.5.4 实现约束

- `_DashboardPageState` 混入 `WidgetsBindingObserver`，在 `initState` 注册、`dispose` 取消注册。
- 不引入新的后端接口，不改变权限、错误码与幂等语义。
- 此优化仅影响 Android 客户端 `presentation` 层，不影响 Windows 端。

#### 22.5.5 验收标准

1. 将 App 切到后台再切回来，首页看板与批次预警自动刷新。
2. 30 秒内多次进出首页不会发出多余请求。
3. 完成一笔入库/出库后返回首页，看板数据立即更新。
4. 从商品管理页返回后，批次效期监控卡片数据刷新。

---

## 5. 序列号追踪模块（Serial Number Tracking，v1.6.0 新增）

### 5.1 背景与目标
对高值、可单台追踪的商品（如手机、电子设备），支持逐台录入序列号（SN/IMEI），
实现「哪台设备从哪批货入库 → 卖给哪個客户」的完整链路。

### 5.2 商品配置

| 字段 | 类型 | 说明 |
|---|---|---|
| `track_batches` | bool | 追踪批次（已有） |
| `track_serials` | bool | 每件独立SN码（新增） |

> 两字段独立，可同时开启（例：医疗器械既需批次召回也需单台追踪）。
> `track_serials=true` 且有在库 SN 时，不允许关闭该开关。

### 5.3 数据模型

```
serial_numbers
├── id          UUID PK
├── tenant_id   UUID
├── sn          TEXT（同租户唯一）
├── product_id  BIGINT → 商品
├── batch_id    UUID?  → 批次（可选）
├── status      TEXT: IN_STOCK | SOLD | RETURNED
├── unit_cost   NUMERIC 入库进价快照
├── sell_price  NUMERIC 出库售价快照
├── inbound_biz_no  TEXT
└── outbound_biz_no TEXT
```

### 5.4 后端接口

| 接口 | 方法 | 权限 | 描述 |
|---|---|---|---|
| `/serials/inbound` | POST | OWNER/ADMIN/PURCHASER | 序列号入库（批量 SN 绑定商品） |
| `/serials/outbound` | POST | OWNER/ADMIN/SALES | 序列号出库（扫 SN 直接出库） |
| `/serials` | GET | 全角色 | 按 product_id 或 sn 查询 |

**错误码**：
- `4093`：SN 已在库，不能重复入库
- `4094`：SN 当前状态非 IN_STOCK，不能出库

### 5.5 入库流程（App）

```
1. 选择商品（仅显示 track_serials=true 的商品）
2. 可选：选择/新建批次（track_batches=true 时）
3. 填统一进货价（可选）
4. 连续扫码模式逐台扫 SN（仅支持连续扫码，不支持 scan_confirm）
   - 重复 SN 实时拒绝，触觉反馈区分
   - 支持撤销最后一条
5. 点击「提交入库」，批量写入系统，current_stock += SN 数量
```

### 5.6 出库流程（App）

```
直接扫 SN → 系统自动识别商品型号 → 加入出库列表
（无需先选商品，SN 本身携带 product_id 信息）
提交出库：status=SOLD，current_stock -= SN 数量
```

### 5.7 验收标准

1. 同一 SN 不能重复入库（除非已 RETURNED），返回 code=4093。
2. 只有 IN_STOCK 状态的 SN 可以出库，其他状态返回 code=4094。
3. 入库/出库均为事务原子性，任一 SN 失败则整批回滚。
4. 出库时 SN 可反查商品名称。
5. `track_serials` 开关与 `track_batches` 可独立控制，互不影响。

### 5.8 App 实现细节（v1.6.0）

#### 商品配置
- `products_edit_page.dart` 新增「每件独立 SN 码」`SwitchListTile`
- 与「启用批次追踪」并列，独立控制，不互斥

#### 数据层
- `serial_models.dart`：`SerialNumber`、`SerialInboundRequest/Result`、`SerialOutboundRequest/Result`、`SerialScanEntry`
- `serial_repository.dart`：封装 `POST /serials/inbound`、`POST /serials/outbound`、`GET /serials`

#### 序列号入库页（`serial_inbound_page.dart`）
- **三步流程**：选商品（仅显示 track_serials=true）→ 填进货价 → 连续扫码
- 连续扫码模式：`MobileScanner` 持续识别，每次扫码自动判重
- 重复 SN：`HapticFeedback.mediumImpact()` + 红色提示，不加入有效列表
- 撤销：移除最新一条有效 SN
- 提交：整批 SNs 一次性 POST `/serials/inbound`

#### 序列号出库页（`serial_outbound_page.dart`）  
- **直接扫 SN**：无需选商品，扫码后实时调用 `GET /serials?sn=xxx` 验证在库状态
- 非 IN_STOCK 状态拒绝添加，振动反馈
- 出售价通过右上角按钮展开填写（可选）
- 提交：整批 SNs 一次性 POST `/serials/outbound`

#### 首页入口
- 功能 Tab 新增「序列号入库」（蓝色）和「序列号出库」（粉色）快捷卡片
- 从 canInbound/canOutbound 权限控制可见性

### 5.9 修复记录（v1.6.1）

**Bug**：`persistence.rs` 未注册 migration `0015` 和 `0016`，导致服务重启时
`track_serials` 列和 `serial_numbers` 表永远不会被创建，商品列表 API 返回 code=5000。

**Fix**：在 `persistence.rs` 的 `apply_postgres_migrations()` 数组中补充：
- `0015_drop_version_columns`
- `0016_serial_numbers`

**规范**：每次新增 SQL migration 文件后，**必须同步在 `persistence.rs` 中注册 `include_str!` 条目**，否则服务端永远不会执行该 DDL。

### 5.10 修复记录（v1.6.2）

**Bug**：migration 0015 使用 `DROP COLUMN`（无 IF EXISTS），在列已不存在时报错，
导致服务每次启动时 migration 执行失败，所有接口返回 code=5000。
另外 schema_migrations 表在 0013~0016 DDL 实际执行后未记录版本号，
导致服务每次重启都重试这些已执行的 migration。

**Fix**：
1. `0015` 改为 `DROP COLUMN IF EXISTS`（幂等）
2. 手动向 schema_migrations 插入 0013~0016 的记录

### 5.11 修复记录（v1.6.3）

**Bug**：migration 0015 删除了 `version` 列，但 repository.rs 里所有 SQL 查询都还在 SELECT `version`，导致所有商品/订单接口返回 code=5000。

**Fix**：新增 migration 0017 `restore_version_columns`，用 `ADD COLUMN IF NOT EXISTS` 把 version 列恢复到 products / purchase_orders / sales_orders / stock_checks 四张表。

**规范**：删除业务列前，必须先审查全部 SQL 语句确认没有引用。

### 5.12 修复记录（v1.6.4）

**Bug**：「序列号入库/出库」卡片只加在了 quickActions 列表里，但功能 Tab 的 UI 是手工拼接的，并未读该列表渲染卡片，所以用户在功能 Tab 看不到这两个入口。

**Fix**：在功能 Tab 的 ListView 中新增「序列号管理」分区，手工加入两张 _QuickActionCard。

### 5.13 UI 优化（v1.6.5）

**变更**：销售出库和采购入库从原来的 `_ScanModuleCard`（三行列表卡）改为 `_ScanToggleCard`（带底部模式切换的网格卡）。
功能 Tab 整体统一为 2列 GridView 卡片布局，去掉「进入完整表单」入口。

**交互**：点底部「确认」/「连续」胶囊切换模式，点卡片主区域以选定模式进入。

### 5.14 UX 优化（v1.6.6）

**变更**：`_ScanToggleCard` 底部模式切换胶囊标签由「确认/连续」改为「逐件扫/批量扫」，更直白易懂。

### 5.15 UI 重构（v1.6.7）

**变更**：去掉「序列号管理」独立分区，将「SN 出库」和「SN 入库」并入「出入库」分区。
出入库区域结构：上方为普通出库/入库（ScanToggleCard，含逐件/批量切换），下方为 SN 出库/SN 入库（小型 QuickActionCard）。

### 5.16 修复记录（v1.6.8）

**Bug**：序列号入库提交失败时（如存在重复 SN）没有任何提示，静默失败。

**Fix**：
1. 后端 `serial_inbound` 改为汇总所有重复 SN 后统一返回（而非遇到第一个就中断），错误消息格式变为「以下序列号已在库，不能重复入库：SN1、SN2」
2. 前端 `_submit()` catch 块改用 `ScaffoldMessenger.showSnackBar` 显示持续 6 秒的错误红色提示，含「知道了」按钮

### 5.17 新功能（v1.6.9）

**新增**：SN 历史记录功能。

后端：
- `SerialNumber` 模型加 `created_at` / `updated_at`
- 新增 `GET /serials/history?status=&page=&page_size=` 接口
- `list_serials_history` 方法支持全局分页、状态过滤

前端：
- 新增 `SerialLogsPage`（全部/在库/已出库三个 Tab，分页翻页）
- 在「序列号入库」和「序列号出库」AppBar 加「历史」(🕐) 按钮

### 5.18 修复记录（v1.6.10）

**Bug**：销售出库 `更新商品库存失败 code=5000`。

**根因**：`repository.rs` 中 `outbound()` 函数的 UPDATE products SQL 占位符编号错误：
```sql
-- 错误写法（$3/$4 对应不上 bind 顺序）
SET current_stock = $1, updated_at = NOW()
WHERE tenant_id = $3 AND id = $4

-- 正确写法
SET current_stock = $1, updated_at = NOW()
WHERE tenant_id = $2 AND id = $3
```

同时修复了批量入库及退货相关的同类 SQL 占位符错位问题（共 3 处）。

### 5.19 UI 优化（v1.6.11）

**变更**：出库结果卡片改版。
- 提交前快照 productId→name 映射
- 结果列表每行显示商品名称，替代原来的「商品ID=xxx」
- 总金额加 ¥ 前缀，字体放大、改为主色
- 每行格式：商品名 + 出库数量 + 剩余库存

### 5.20 扫码防抖修复（v1.6.12）

**Bug**：SN 入库/出库扫码器无防抖，与连续扫码组件的 1500ms 时间窗口不一致，导致镜头停留时重复识别同一码。

**修复**：在 `serial_inbound_page.dart` 和 `serial_outbound_page.dart` 的 `_onDetect` 方法中加入 1500ms 时间窗口防抖（`_lastScanCode` + `_lastScanAtMs`），与 `barcode_scanner_sheet.dart` 连续扫码实现保持一致。

### 5.21 订单下钻显示 SN 码（v1.6.13）

**功能**：订单下钻页面，每个明细行下方显示出库的 SN 码列表（仅 `track_serials=true` 的商品有值）。

**后端**：`SalesOrderItemData` 新增 `sns: Vec<String>` 字段（空时不序列化）；`get_dashboard_orders_drilldown` 在分页后批量 JOIN `serial_numbers` 表（按 `outbound_biz_no` + `product_id`）填充 SN 列表。

**前端**：`SalesOrderItemData` 加 `sns: List<String>`；`_ItemRow` 在商品行下方以彩色徽章形式展示 SN 码。

**Bug 修复**：上一版本 Python 脚本写入 Flutter Dart 文件时将 `$` 误转义为 `\$`，导致订单明细行的单价和小计显示为字面字符串 `¥${item.sellPrice}` 而非实际金额。已修复 `_ItemRow` 中三处插值。

### 5.22 订单下钻 UI 全面优化（v1.6.14）

**A. Header 重构**：去掉独立的绿色总额区块，将总额 `¥x.xx` 合并到 Header 第二行右侧（时间左、总额右），减少卡片约 30% 高度。

**B. 表头列宽优化**：去掉固定宽度的单价列，改为：商品名（flex:6）+ 数量（36px）+ 小计（72px）。单价改为每行第二行小字副信息展示，窄屏不溢出。

**C. 明细行浅底色分组**：每条明细用 `surfaceContainerLowest` 背景 + 圆角区分；相邻两行之间加极细分割线（50% 透明）。

**D. 空明细文案友好化**：「无明细（流水聚合行）」改为「由出库记录聚合，无关联销售单据」，加斜体和图标，更易懂。聚合行本身也在 Header 中以小字注释。

### 5.23 临期批次提醒下钻修复（v1.6.15）

**Bug**：临期批次提醒点击后只跳到第一个商品的批次管理页，多商品时其余批次不可见。

**修复**：新增 `ExpiringBatchesPage`（临期批次汇总页），按商品分组展示所有 30 天内到期的批次，每条批次显示批次号、到期日、供应商及剩余天数徽章（颜色区分紧迫程度）；点击商品标题可跳转到对应商品的批次管理页。首页点击临期提醒卡片改为跳转此新页面。

### 5.24 商品详情页（v1.6.16）

**功能**：新增独立商品详情页 `ProductDetailPage`，商品列表卡片点击后跳入详情页。

**详情页内容**：
- Hero 名称卡（渐变背景，低库存时变红色警告）
- 库存状态卡（进度条 + 当前库存 / 预警阈值 / 缺货数量三格统计）
- 价格信息卡（零售价 / 成本价 / 毛利率 / 最近进货价，仅有权限时显示）
- 基本信息卡（SKU / 条码可复制 / 分类 / 商品 ID / 功能标签 Chip）
- 操作区（编辑 / 批次管理 / 发起盘点 / 删除，含权限控制）

**商品列表卡片**：简化为轻量版，去掉操作按钮行，整张卡片可点击，右侧加箭头图标指示可进入详情。

### 5.25 分类筛选改为横向 Chip 栏（v1.6.17）

**改动**：商品管理页的分类筛选从底部弹窗（InputDecorator）改为常驻横向滚动 Chip 栏，支持三级联动。

**交互**：
- 第一行：所有大类 Chip + 「全部」+ 管理入口（⚙）
- 选中大类后，第二行出现中类（含「全部大类名」Chip）
- 选中中类后，第三行出现小类
- 点击选中的 Chip 退回上级
- 每次点击即时触发商品查询，无需额外确认

**_FilterCard**：移除分类筛选相关参数，仅保留关键字/条码搜索。
