# 🏗️ 架构设计文档（ADD）- SaaS 进销存系统

| 文档属性 | 内容 |
| --- | --- |
| 项目名称 | 极速云进销存（Hyper JXC） |
| 文档版本 | v1.4.1（新增：商品 `track_batches` 字段、入库批次关联弹窗架构） |
| 技术栈 | Rust(Axum) + Tauri + Flutter + PostgreSQL + Redis |
| 架构模式 | 前后端分离 + 多租户 SaaS + 分层架构 |

---

## 1. 设计目标与原则

### 1.1 设计目标
1. 支撑采购入库、销售出库、库存盘点三条核心业务链路。
2. 保证库存与成本计算在并发场景下的强一致性。
3. 支撑多租户隔离与角色权限控制。
4. 支撑 Android 端弱网离线作业与幂等重试。

### 1.2 核心原则
- **一致优先**：库存与资金相关写操作必须事务化。
- **租户隔离**：任何查询/写入都必须绑定 `tenant_id`。
- **契约先行**：API 统一响应结构与字段命名。
- **可追溯**：库存变更必须写不可变流水日志。
- **可恢复**：写接口幂等，支持离线重放。

---

## 2. 系统逻辑架构（Layered Architecture）

### 2.1 架构分层

1. **客户端层**
   - **Windows（Tauri + Vue3）**：商品管理、采购/销售开单、报表、打印。
   - **Android（Flutter）**：扫码入库、移动开单、盘点、离线队列。

2. **接入层**
   - **Nginx**：TLS 终止、反向代理、限流、静态资源压缩。
   - **API Middleware**：鉴权、租户上下文注入、请求追踪、幂等校验。

3. **应用服务层（Rust + Axum）**
   - `auth`：登录/刷新/登出
   - `product`：商品与价格、预警阈值
   - `inventory`：入库/出库/盘点
   - `report`：经营看板与统计汇总

4. **数据层**
   - **PostgreSQL**：主数据与事务数据
   - **Redis**：热点缓存、会话/黑名单、短期幂等键缓存（可选）

---

## 3. 多租户与权限架构

## 3.1 租户隔离策略
- 共享数据库、共享 Schema，通过 `tenant_id` 做逻辑隔离。
- JWT 载荷包含：`tenant_id`、`user_id`、`role`。
- 鉴权中间件解析 Token 并注入 `TenantContext` 到请求上下文。

```rust
pub struct TenantContext {
    pub tenant_id: Uuid,
    pub user_id: Uuid,
    pub role: UserRole,
}
```

## 3.2 RBAC 角色模型
- `OWNER`：租户全权限
- `PURCHASER`：商品、采购、库存
- `SALES`：商品只读、销售开单

> 权限校验顺序：鉴权 -> 租户校验 -> 角色校验 -> 数据操作。

## 3.3 操作级权限建议（高风险）
- 查看成本价：仅 `OWNER/PURCHASER`
- 单据作废：需满足状态机规则 + 操作审计
- 报表导出：按角色与模块范围授权
- 员工权限变更：仅 `OWNER`

---

## 4. 领域模型与数据库设计

## 4.1 核心实体关系
- `tenants (1) -> (N) users`
- `tenants (1) -> (N) products`
- `tenants (1) -> (N) suppliers`
- `tenants (1) -> (N) customers`
- `products (1) -> (N) stock_logs`
- `purchase_orders (1) -> (N) purchase_order_items`
- `sales_orders (1) -> (N) sales_order_items`
- `stock_checks (1) -> (N) stock_check_items`

## 4.2 核心表结构（节选）

```sql
CREATE TABLE products (
    id BIGSERIAL PRIMARY KEY,
    tenant_id UUID NOT NULL,
    sku VARCHAR(64) NOT NULL,
    barcode VARCHAR(64) NOT NULL,
    name VARCHAR(255) NOT NULL,
    unit VARCHAR(32) NOT NULL DEFAULT '件',

    cost_price DECIMAL(16,4) NOT NULL DEFAULT 0,
    retail_price DECIMAL(16,4) NOT NULL DEFAULT 0,
    wholesale_price DECIMAL(16,4) NOT NULL DEFAULT 0,
    last_inbound_unit_cost DECIMAL(16,4) NULL,
    current_stock INT NOT NULL DEFAULT 0,
    min_stock_limit INT NOT NULL DEFAULT 0,

    image_url TEXT,
    version INT NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    UNIQUE (tenant_id, sku),
    UNIQUE (tenant_id, barcode)
);

CREATE INDEX idx_products_tenant_name ON products (tenant_id, name);
CREATE INDEX idx_products_tenant_barcode ON products (tenant_id, barcode);

CREATE TABLE stock_logs (
    id BIGSERIAL PRIMARY KEY,
    tenant_id UUID NOT NULL,
    product_id BIGINT NOT NULL,
    biz_type VARCHAR(32) NOT NULL,        -- IN_PURCHASE/OUT_SALE/ADJ_CHECK/RETURN
    biz_no VARCHAR(64) NOT NULL,
    delta_qty INT NOT NULL,
    snapshot_stock INT NOT NULL,
    snapshot_cost DECIMAL(16,4) NOT NULL,
    snapshot_inbound_unit_cost DECIMAL(16,4) NULL,
    snapshot_sell_price DECIMAL(16,4) NULL,
    operator_id UUID NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE idempotency_records (
    id BIGSERIAL PRIMARY KEY,
    tenant_id UUID NOT NULL,
    idempotency_key VARCHAR(128) NOT NULL,
    method VARCHAR(16) NOT NULL,
    path VARCHAR(128) NOT NULL,
    response_code INT NOT NULL,
    response_body JSONB NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (tenant_id, idempotency_key, method, path)
);

CREATE TABLE audit_logs (
    id BIGSERIAL PRIMARY KEY,
    tenant_id UUID NOT NULL,
    operator_id UUID NOT NULL,
    action VARCHAR(64) NOT NULL,            -- UPDATE_PRICE / VOID_ORDER / UPDATE_STOCK_LIMIT ...
    target_type VARCHAR(32) NOT NULL,       -- product / purchase_order / sales_order / user
    target_id VARCHAR(64) NOT NULL,
    before_data JSONB,
    after_data JSONB,
    request_id VARCHAR(64) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

> 说明：`stock_logs` 为审计日志表，仅允许新增，不允许更新/删除。

## 4.3 条码元数据缓存表（本次新增）

为支撑“新建商品时自动查询建议名称”，新增本地缓存表，采用 Cache-Aside + 负缓存：

```sql
CREATE TABLE barcode_lookup_cache (
    id BIGSERIAL PRIMARY KEY,
    tenant_id UUID NOT NULL,
    barcode VARCHAR(64) NOT NULL,
    lookup_status VARCHAR(16) NOT NULL,         -- FOUND / NOT_FOUND
    product_name VARCHAR(255) NULL,
    source VARCHAR(32) NOT NULL DEFAULT 'THIRD_PARTY',
    raw_payload JSONB NOT NULL DEFAULT '{}'::jsonb,
    expires_at TIMESTAMPTZ NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (tenant_id, barcode)
);

CREATE INDEX idx_barcode_lookup_cache_tenant_expire
  ON barcode_lookup_cache (tenant_id, expires_at);
```

约束：
1. 必须按 `tenant_id + barcode` 隔离；
2. `FOUND` 与 `NOT_FOUND` 使用不同 TTL（负缓存 TTL 更短）；
3. `raw_payload` 用于故障排查与供应商切换验证。

## 4.4 商品进货价格默认值持久化（本次新增）

为满足“入库页进货价格默认值跨端一致且可沿用上次入库修改值”，在商品维度引入持久化字段：

1. 字段定义
   - `products.last_inbound_unit_cost DECIMAL(16,4) NULL`
   - 语义：仅用于“下一次入库表单默认进货价格”，不替代 `cost_price`。

2. 初始化与演进
   - 新建商品时可为空；客户端默认值按 `last_inbound_unit_cost ?? cost_price` 读取。
   - 首次入库后写入本次有效进价，后续可持续覆盖为最近一次入库有效进价。

3. 更新口径
   - 入库有效进价：`effective_unit_cost = unit_cost ?? product.cost_price`
   - 每次 `IN_PURCHASE` 成功提交后，同事务更新：
     - `products.last_inbound_unit_cost = effective_unit_cost`
     - `products.cost_price` 继续按移动加权公式更新。

---

## 5. 核心事务与一致性设计

## 5.1 入库事务（采购/扫码入库）
步骤：
1. 校验参数与权限。
2. 开启事务。
3. `SELECT ... FOR UPDATE` 锁定 `products` 行。
4. 计算本次有效成本 `effective_unit_cost`：优先使用请求 `unit_cost`；若未传或为空则回落到商品当前 `cost_price`。
5. 计算 `cost_price`（移动加权平均法）。
6. 更新 `current_stock`、`cost_price` 与 `last_inbound_unit_cost`。
7. 写入 `stock_logs`。
8. 提交事务。

> 说明：入库页“进货价格”默认值读取优先级为 `last_inbound_unit_cost`，为空时回落 `cost_price`。

公式：

```text
new_cost_price = ((old_stock * old_cost_price) + (in_qty * effective_unit_cost)) / (old_stock + in_qty)
```

## 5.1.1 批量入库事务（本次新增）

新增写接口：`POST /inventory/inbound/batch`，用于一次提交多条入库明细，核心约束如下：

1. **整批原子性**
   - 服务端必须在单个数据库事务中处理整批 `items[]`；
   - 任一明细校验失败（如商品不存在、版本冲突、参数非法）则整批回滚；
   - 禁止出现“前几条成功、后几条失败”的部分提交状态。

2. **单条处理语义复用**
   - 每条明细沿用单条入库成本口径：`effective_unit_cost = unit_cost ?? product.cost_price`；
   - 每条明细均执行 `FOR UPDATE` 行锁、移动加权成本计算、库存更新、版本递增与库存流水写入。

3. **统一业务单号**
   - 整批请求由服务端生成一个 `biz_no`；
   - 批次内所有库存流水共用该 `biz_no`，便于追踪与审计。

4. **响应语义**
   - 返回结构按批次输出：`biz_no + items[]`；
   - `items[]` 每项包含：`product_id/current_stock/cost_price/version`，用于客户端批量回填。

5. **客户端编排约束（本次补充）**
   - 入库页需支持多商品明细暂存与合并（同商品累加、异商品新增行）；
   - 当暂存明细数量 >= 2 时，默认走 `POST /inventory/inbound/batch`；
   - 保留 `POST /inventory/inbound` 单条兼容路径。

## 5.2 出库事务（销售）
1. 开启事务。
2. 对涉及商品逐条 `FOR UPDATE`。
3. 校验 `current_stock >= qty`（默认不允许负库存）。
4. 扣减库存并写 `stock_logs`。
5. 写销售订单及明细。
6. 提交事务。

## 5.3 盘点调整事务
- 盘点确认时，根据实盘与账面差生成 `delta_qty`。
- 更新库存并写 `ADJ_CHECK` 类型流水。

## 5.4 单据状态机约束（建议强制实现）
- 采购单：`DRAFT -> CONFIRMED -> VOIDED`
  - `CONFIRMED` 后不可直接删除，作废需写反向库存流水。
- 销售单：`DRAFT -> CONFIRMED -> RETURNED_PARTIAL/RETURNED_FULL`
  - 退货必须通过专用退货流程冲销库存与金额。
- 盘点单：`DRAFT -> COUNTING -> CONFIRMED`
  - `CONFIRMED` 后禁止修改盘点明细。

## 5.5 离线冲突与版本控制
- 写请求建议携带 `expected_version`。
- 服务端版本校验失败返回 `4091`，并附最新资源快照。
- 客户端将冲突任务标记“待处理”，禁止静默覆盖。

## 5.6 条码回源与降级策略（本次新增）

1. 请求 `GET /products/barcode-lookup?barcode=...` 时先查本地缓存：
   - 命中且未过期：直接返回 `cache_hit=true`；
   - 未命中或已过期：回源第三方条码 API。
2. 回源成功：
   - 有名称：写 `FOUND` + 长 TTL；
   - 无名称：写 `NOT_FOUND` + 短 TTL（负缓存）。
3. 回源失败降级：
   - 若存在过期缓存，可返回 `stale` 数据并标记 `source=CACHE_STALE`；
   - 若无缓存，返回 `NOT_FOUND`（不阻断手工建档）。
4. 该链路为读操作，不引入写幂等键要求，继续遵循统一响应结构。

---

## 6. API 契约基线（与 API 文档强一致）

## 6.1 统一响应结构

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "request_id": "req_9f8d..."
}
```

- 约束补充：对于 Axum 提取器层（如 `Json<T>`）产生的请求体解析错误（典型 HTTP 422），必须通过统一错误处理映射为标准 JSON 响应体，禁止透传默认纯文本错误。
- 建议映射口径：HTTP 状态可保持 `400/422`（按实现选择），业务码统一 `4000`，并包含 `request_id` 与可读 `message`。

## 6.2 通用规则
- 写接口必须携带 `X-Idempotency-Key`。
- 分页参数统一：`page`, `page_size`。
- 分页响应统一：`list`, `total`, `page`, `page_size`。
- 库存字段统一命名：`current_stock`。
- 成本字段语义：`unit_cost`（请求入参）/`cost_price`（商品当前成本）。
- 商品创建接口 `POST /products` 中 `cost_price` 为必填字段，缺失或空值按参数错误处理。
- 采购入库接口 `POST /inventory/inbound` 中 `unit_cost` 为可选字段，不传或空值时回落商品当前 `cost_price` 参与成本计算。
- 批量入库接口 `POST /inventory/inbound/batch` 中每条 `items[*].unit_cost` 同样为可选字段；不传或空值时按该商品当前 `cost_price` 回落。
- 商品读取对象需返回 `last_inbound_unit_cost`（可空），用于客户端入库默认值：`last_inbound_unit_cost ?? cost_price`。

## 6.3 幂等重放策略
- 相同 key + 相同请求体：返回首次响应，并可设置 `X-Idempotent-Replay: true`。
- 相同 key + 不同请求体：返回 `4092` 幂等冲突。
- 幂等记录至少保留 24 小时（可配置）。

## 6.4 业务单号（`biz_no`）服务端自动生成策略（本次新增）

1. **创建接口统一由服务端生成 `biz_no`**
   - 采购入库：`POST /inventory/inbound`
   - 销售出库：`POST /inventory/outbound`
   - 创建采购单：`POST /purchase-orders`
   - 创建销售单：`POST /sales-orders`
   - 创建盘点单：`POST /inventory/stock-checks`

2. **分层职责**
   - 路由/Application 层：生成 `biz_no`，并将其传入 Repository。
   - Repository/持久层：保持对 `biz_no` 的存储与唯一约束，不从客户端信任该字段。
   - 返回层：所有创建响应 `data.biz_no` 返回系统最终生成值。

3. **兼容策略**
   - 老版本客户端若继续传 `biz_no`，服务端可接收但**不作为创建主依据**（以服务端生成值为准）。
   - 若业务需记录外部纸单号，后续新增独立字段（如 `external_ref_no`），避免复用 `biz_no`。
   - Windows/Android 新版本客户端创建请求不再主动传 `biz_no`，统一使用响应 `data.biz_no` 作为展示与追踪依据。

4. **唯一性保障**
   - 仍保留数据库侧 `tenant_id + biz_no` 唯一索引作为最终防线。
   - 生成后若命中冲突，服务端进行重试生成（有限重试）再入库。

## 6.5 条码查询接口契约补充（本次新增）

1. 新增接口：`GET /products/barcode-lookup`。
2. 典型响应字段：
   - `barcode`
   - `status`（`FOUND` / `NOT_FOUND`）
   - `suggested_name`（可空）
   - `cache_hit`（布尔）
   - `source`（`CACHE` / `THIRD_PARTY` / `CACHE_STALE` / `DEGRADED`）
3. 接口定位：用于新建商品辅助命名，不替代既有 `/products/scan` 的“本地商品查找”语义。

## 6.6 扫码文案与行为一致性约束（本次新增）

1. 仅当交互动作会直接拉起摄像头扫码面板时，按钮/tooltip 才允许使用“扫码”动词。
2. 对“处理当前输入框条码（手输/扫码枪）”动作，统一使用“按条码处理/按条码加入明细/按条码查询”等文案。
3. `continuous_scan` 作为模式名可保留；但会话内手动触发处理按钮仍需遵守第 2 条，避免误导为“点击后才会拉起摄像头”。
4. 本约束仅涉及 Presentation 文案层，不改变 API 契约、权限语义、错误码语义与请求参数。

## 6.7 扫码模式 Tab 化与智能触发约束（本次新增）

1. 入库/出库页的扫码模式切换控件统一采用并排 Tab（或分段按钮），替代下拉框，提升可见性与切换效率。
2. 扫码模式值收敛为：`scan_confirm / continuous_scan`，仅调整前端展示与交互控件，不改变组包字段。
3. Android 端仅在 `scan_confirm` 模式下，允许主按钮在“条码输入为空”时智能拉起摄像头；识别成功后继续原处理流程。
4. `continuous_scan` 模式保持显式开启会话行为，不由主按钮空输入智能触发替代。
5. 兼容迁移：若读取到历史模式值 `quick_accumulate`，客户端需自动映射为 `continuous_scan` 后再渲染与持久化。
6. 上述改造均属于客户端 Presentation 层增强，不新增后端接口，不变更错误码与权限语义。

## 6.8 经营看板统计口径约束（本次新增）

1. `GET /reports/dashboard` 全指标统一按租户时区自然日统计（当前实现为 `UTC+8`）。
2. `total_sales / total_gross_profit / top_selling_item` 统一基于当日销售流水计算：
   - 仅统计 `OUT_SALE` 与 `RETURN_SALE`；
   - 单价口径采用“快照优先、映射兜底”：优先使用 `stock_logs.snapshot_sell_price`，快照为空时回退 `(biz_no, product_id) -> sales_orders.items.sell_price`。
3. `total_orders` 口径统一为：当日满足“存在有效销售单价（快照或映射）”的有效 `OUT_SALE` 流水中，唯一 `biz_no` 数量。
4. 不再以 `sales_orders.confirmed_at` 直接计数订单数，避免“已确认但无销售流水”导致看板偏差。
5. “快照为空且无法映射”的历史/遗留流水必须忽略，不得进入销售额、毛利、订单数与热销品统计。

## 6.9 经营看板订单数下钻架构约束（本次新增）

1. 新增只读接口 `GET /reports/dashboard/orders`，用于承载“看板订单数”下钻列表查询。
2. 该接口必须复用看板订单数口径：
   - 仅统计 `OUT_SALE`；
   - 且 `(biz_no, product_id)` 存在有效销售单价（快照或映射）；
   - 按唯一 `biz_no` 聚合得到订单集合。
3. 查询参数采用日期范围：`start_date/end_date`（均可缺省为当天，若提供则必须成对且 `start_date <= end_date`），并支持分页 `page/page_size`。
4. 返回列表按 `biz_no` 维度输出，`total` 必须与同口径下钻订单总数一致，确保可与看板 `total_orders` 做一致性校验。
   - 若 `biz_no` 在 `stock_logs` 中存在有效 `OUT_SALE`，但不存在 `sales_orders` 主记录，接口必须返回由库存流水聚合生成的“聚合行”；
   - 聚合行约束：`status=OUTBOUND_ONLY`，并按 `(biz_no, product_id, sell_price)` 聚合回填 `items`；仅在历史数据无法聚合时才允许 `items=[]`，`remark` 标注“由库存流水聚合生成（无销售单主记录）”。
5. 权限边界：接口仅开放 `OWNER/SALES`，避免非销售角色通过接口绕过页面权限直接访问订单下钻明细。
6. Windows 前端联动策略：
   - 看板“订单数”点击后路由跳转至销售单页面并透传 `start_date/end_date`；
   - 看板“订单数”点击后，前端应按看板 `total_orders` 动态设置首屏 `page_size`（`1~100`，超出按 `100`）；
   - 销售单页面在接收到下钻参数后自动触发列表查询；
   - 点击热区不应仅限数字文本，建议整张“订单数”指标卡可点击；
   - 无权限点击时必须给出显式提示（例如“仅 OWNER / SALES 可查看订单下钻”），不得静默无反馈；
   - 路由跳转失败（含前端路由异常）必须回显失败提示，避免用户感知为“点击无反应”；
   - 用户可修改日期范围二次筛选，仍复用同一后端接口。
7. Android 前端联动策略：
   - 看板“今日订单数”指标卡在 Android 端必须支持点击反馈，不允许静默无响应；
   - `OWNER/SALES` 点击后进入订单下钻页，默认带入 `start_date=end_date=dashboard.date`；
   - Android 首屏 `page_size` 默认固定为 `10`（与 Windows 动态策略解耦）；
   - 用户手动修改每页条数时，若输入为空或非法，客户端回落为 `10`；
   - `PURCHASER` 点击时必须即时提示“当前角色无销售单下钻权限，仅 OWNER / SALES 可查看”；
   - 下钻页查询失败、跳转失败、日期范围不合法（格式错误或开始晚于结束）均需前端即时可读提示；
   - 用户可在下钻页修改日期范围并分页查询，继续复用 `GET /reports/dashboard/orders`。
    - "今日销售额"与"今日毛利润"指标卡点击后进入销售/毛利趋势分析页（见 7.2.P）。

## 6.16 经营趋势查询接口架构约束（本次新增）

1. 新增只读接口 `GET /reports/trend`，用于返回指定日期范围内的每日经营汇总数据。
2. 该接口必须复用看板 `GET /reports/dashboard` 的统计口径：
   - 仅统计满足"存在有效销售单价"的 `OUT_SALE/RETURN_SALE` 流水；
   - 快照优先、映射兜底；
   - 无效流水忽略。
3. 查询参数：`start_date/end_date`，缺省时默认近 7 天。
4. 返回结构：`days[]` 数组，每个元素包含 `date/total_sales/total_gross_profit/total_orders/low_stock_count/top_selling_item`。
5. 日期范围上限建议 90 天，防止性能问题。
6. 若某天无任何销售流水，仍须返回该天记录，各指标为零值。
7. 权限边界：仅 `OWNER/SALES` 可访问，`PURCHASER` 返回 `4030`。
8. 后端实现建议：
   - 可优化为单次 SQL 按日期分组聚合，减少多次循环查询的数据库往返；
   - 内部复用看板的核心统计逻辑，确保口径一致。

## 6.10 订单小票化展示字段架构约束（本次新增）

1. 销售单响应明细项（`SalesOrderData.items[]`）新增：
   - `product_name`：商品名称；
   - `line_amount`：行小计，计算口径 `qty * sell_price`，按 4 位小数输出字符串。
2. 采购单响应明细项（`PurchaseOrderData.items[]`）新增：
   - `product_name`：商品名称；
   - `line_amount`：行小计，计算口径 `qty * unit_cost`，按 4 位小数输出字符串。
3. 采购单响应对象（`PurchaseOrderData`）新增：
   - `total_amount`：订单总金额，口径为各明细 `line_amount` 求和，按 4 位小数输出字符串。
4. 商品名称快照与读取优先级策略：
   - 创建采购单/销售单时，服务端按明细维度固化 `product_name_snapshot`（订单创建时的商品名称快照）；
   - 读取订单明细 `product_name` 时优先使用快照字段：`product_name_snapshot`；
   - 当快照为空（历史数据兼容）时，再回退租户内 `product_id -> product_name` 主数据映射；
   - 当映射仍缺失时，返回兜底展示值：`商品#<product_id>`。
5. 本增强仅扩展既有响应字段，不新增接口路径，不改变权限、幂等、错误码及状态机语义。

## 6.11 看板订单下钻聚合行明细回填约束（本次新增）

1. 针对 `GET /reports/dashboard/orders` 中 `OUTBOUND_ONLY` 聚合行，服务端需基于库存流水按 `(biz_no, product_id, sell_price)` 聚合生成明细项。
2. 聚合明细字段与销售单明细结构保持一致：`product_name / qty / sell_price / line_amount / returned_qty`。
3. 商品名称优先读取商品主数据映射，缺失时兜底 `商品#<product_id>`。
4. 仅当历史流水缺失关键字段导致无法聚合时，才允许 `items=[]` 并由客户端展示“无明细（聚合行）”提示。
5. 本约束不改变下钻接口查询参数、权限边界、错误码与幂等语义。

## 6.12 采购记录只读查询架构约束（本次新增）

1. 新增只读接口 `GET /purchase-orders`，用于采购记录列表查询。
2. 查询参数：`start_date/end_date/page/page_size`，其中：
   - `start_date/end_date` 缺省时默认当天；
   - 若提供日期范围则必须成对且 `start_date <= end_date`；
   - `page` 最小为 `1`，`page_size` 范围 `1~100`，Android 首屏默认 `10`。
3. 权限边界：仅 `OWNER/PURCHASER` 可访问采购记录列表，`SALES` 禁止访问。
4. 返回结构沿用统一分页规范：`list/total/page/page_size`，`list[*]` 复用 `PurchaseOrderData`（含明细与总金额）。
5. 采购记录能力严格只读：
   - 不新增采购单修改接口；
   - 既有采购单写路径仍受状态机与审计约束；
   - 查询接口不引入幂等键要求。
6. 时间口径建议统一租户时区自然日（当前 `UTC+8`）进行日期范围过滤，确保移动端“默认当天”与后端统计一致。

## 6.13 Android 入库记录只读查询架构约束（本次新增）

1. Android 端“采购记录”入口改为“入库记录”入口，复用只读接口 `GET /inventory/logs`。
2. 查询参数约束：
   - 固定过滤：`biz_type=IN_PURCHASE`；
   - 默认筛选：`start_date=end_date=当天`、`page=1`、`page_size=10`；
   - `page_size` 合法范围 `1~100`，非法输入客户端回落 `10`。
3. 权限边界：仅 `OWNER/PURCHASER` 可访问入库记录查询链路，`SALES` 禁止访问。
4. 返回结构沿用库存流水分页规范：`list/total/page/page_size`，`list[*]` 复用 `StockLogData`。
5. 客户端页面能力严格只读：
   - 不提供任何库存写操作入口；
   - 仅承载筛选、分页、展示与错误提示。
6. 日期输入继续使用 Android 日历选择策略，提交参数保持 `YYYY-MM-DD` 契约不变。

## 6.14 入库真实进价快照架构约束（本次新增）

1. `stock_logs` 新增可空字段 `snapshot_inbound_unit_cost`，用于记录“本次入库真实单次进价快照”。
2. 写入语义：
   - `IN_PURCHASE` 相关流水（单条入库、批量入库、采购单确认入库）必须写入该字段；
   - 值来源为本次入库有效进价：`effective_unit_cost = unit_cost ?? product.cost_price`；
   - 非入库业务类型（如 `OUT_SALE/RETURN_SALE/ADJ_CHECK/VOID_PURCHASE`）该字段写 `NULL`。
3. 查询与展示语义：
   - `GET /inventory/logs` 返回 `snapshot_inbound_unit_cost`；
   - Android 入库记录页展示“本次进货价”；字段为空时显示 `--`。
4. 兼容性要求：历史流水允许该字段为空，不影响既有权限、错误码、幂等与统一响应结构。

## 6.15 入库/出库页面表单收敛架构约束（本次新增）

1. 客户端主路径统一收敛为“扫码/明细驱动”：
   - 入库：`扫码 -> 待提交明细 -> 提交入库`；
   - 出库：`扫码/手工明细 -> 明细核对 -> 提交出库`。
2. 入库页手工录入区默认折叠，仅作为异常与纠偏兜底入口。
3. 入库提交动作只允许从待提交明细区触发；不再使用“明细为空时回退单表单直提”的页面策略。
4. 出库页单据头字段（客户、备注、版本）默认折叠；`expected_version` 放入高级字段分组。
5. 上述改造仅影响客户端 `presentation` 编排，不改变后端 API 路径、字段、权限、错误码与幂等语义。

---

## 7. 客户端架构

## 7.1 Windows 端（Tauri + Vue3）
- `axios` 封装请求拦截器（附带 Token、request_id）。
- `Pinia` 管理用户态、系统配置与页面缓存。
- 小票打印通过 Tauri `invoke` 调 Rust 指令。

### 7.1.B 出库页交互模式化与明细编辑策略（本次新增）

1. **出库扫码模式（前端策略）**
   - 默认模式：`scan_confirm`（扫码命中后先弹确认表单，再写入明细）。
   - 可切换会话模式：`continuous_scan`（开启摄像头后持续识别，命中后继续下一条，手动结束后关闭）。
   - 模式切换仅影响前端交互流程，不新增后端接口、不过载额外字段。

2. **连续扫码会话约束（入库/出库共用）**
   - 会话期间每次识别成功后，页面顶部展示成功提示。
   - 会话期间每次识别成功后，触发语音/听觉提示。
   - 同码短时去重（建议 800~1500ms）防止停留误连扫。
   - 提供明确“结束扫码”动作，由用户主动关闭摄像头。

3. **明细初始化策略**
   - 创建页明细集合初始化为空数组，不再预置空首行。
   - 允许通过“新增明细”或“扫码命中”生成首条明细。
   - 提交前维持“至少 1 条有效明细”校验。

4. **订单明细编辑策略（防误触）**
   - 明细行默认只读。
   - 用户点击“编辑”后该行进入可编辑；点击“完成”后恢复只读。
   - 高频扫码写入的新行默认只读，减少误改风险。

5. **确认弹窗一致性（本次补充）**
   - `scan_confirm` 模式下，Windows 出库页必须采用 Modal 承载确认输入；
   - 不再保留页面内联确认卡片。

### 7.1.C 经营看板日期筛选交互策略（本次新增）

1. **默认值策略**
   - Windows 端经营看板页面初始化时，将查询日期设置为客户端当前自然日（`YYYY-MM-DD`）。
   - 首次加载自动以当天日期调用看板接口，减少用户首屏操作。

2. **控件策略**
   - 日期输入采用浏览器原生 Date Picker（`<input type="date">`），优先替代纯文本输入。
   - 页面提交前仍执行格式与合法性校验，统一按 `YYYY-MM-DD` 传递给 API。

3. **契约边界**
   - 此改造仅调整 Presentation 层交互，不修改 `getDashboardApi` 方法签名，不新增后端接口。
   - 继续复用 `GET /reports/dashboard` 的 `date` 查询参数。

### 7.1.D Windows 入库未命中直达建档策略（本次新增）

1. 在 `InboundPage` 扫码入库流程中，若 `/products/scan` 返回 `4040`，前端弹出确认：
   - 主动作：`去新建商品`
   - 次动作：`取消/继续扫码`
2. 选择“去新建商品”后，路由跳转到商品管理页，并携带上下文参数（来源页 + 条码），用于创建表单自动回填。
3. 商品创建成功后，前端回跳入库页并回填 `product_id/barcode`，用户可直接继续录入数量并提交入库。
4. 该流程仅调整前端编排，不新增后端接口，不修改错误码语义。

### 7.1.E Windows 扫码模式记忆与确认续扫策略（本次新增）

1. 入库页与出库页分别持久化扫码模式选择（`scan_confirm / continuous_scan`）。
2. 偏好存储按“用户 + 终端 + 页面”隔离，避免跨用户串用。
3. `scan_confirm` 模式增强为会话式：
   - 每次命中后进入确认表单；
   - 确认写入后默认保持会话，继续等待下一次扫码；
   - 提供显式“结束扫码”动作。
4. 兼容迁移：读取到历史模式值 `quick_accumulate` 时，自动映射并写回 `continuous_scan`。
5. 上述改造属于 Presentation 层与本地存储层增强，不修改后端 API 契约。

### 7.1.F Windows 扫码模式 Tab 化策略（本次新增）

1. 入库/出库页扫码模式切换由下拉框改为并排 Tab（分段按钮），两种模式同时可见。
2. 切换时继续复用既有 `scanMode` 状态与本地偏好存储逻辑，模式值统一为 `scan_confirm / continuous_scan`。
3. 主按钮保持“按条码处理/按条码加入明细”语义，摄像头入口继续由输入框扫码图标承担。
4. 本策略仅调整交互控件与样式，不影响 API 契约与业务状态机。

### 7.1.G Windows 入库确认写入弹窗化策略（本次新增）

1. 仅在入库页 `scan_confirm` 模式启用“确认写入弹窗（Modal/Dialog）”。
2. 扫码命中后弹窗承载确认表单（商品信息 + 数量输入），替代页面内联确认区。
3. 弹窗打开后自动聚焦数量输入框并选中默认值，提升扫码枪 + 键盘连贯性。
4. 键盘策略：
   - `Enter` 触发确认写入；
   - `Esc` 取消本次确认并关闭弹窗。
5. 确认成功后自动回焦扫码输入框，保持“确认后续扫”会话，不要求重复点击主处理按钮。
6. 入库提交成功后重置策略：
   - 清空 `product_id`、`barcode` 与扫码输入框条码，避免误沿用上一商品上下文；
   - 数量字段恢复默认值 `1`，作为下一笔入库初始态；
   - 其余可选字段按既有重置规则清空。
7. 该改造属于 Presentation 层，不变更后端 API、错误码、权限与幂等语义。

### 7.1.H Windows 看板订单下钻首屏可见性策略（本次新增）

1. 从经营看板点击“订单数”下钻时，Windows 前端应根据看板返回的 `total_orders` 动态设置首屏 `page_size`。
2. 首屏 `page_size` 计算规则：`page_size = min(max(total_orders, 1), 100)`。
3. 下钻跳转仍默认带入看板日期：`start_date = end_date = dashboard.date`，并设置 `page=1`。
4. 当当日订单数超过 `100` 时，首屏展示前 `100` 条，剩余数据通过既有分页继续查看。
5. 本策略仅调整 Windows 前端路由 query 组包，不新增后端接口与字段，不改变权限、错误码及分页上限语义。

### 7.1.I Windows 看板订单下钻点击反馈可感知策略（本次新增）

1. 看板“订单数”下钻入口应提供足够点击热区，推荐整张指标卡可点击，不应仅依赖数字文本。
2. 对无下钻权限角色（非 `OWNER/SALES`）点击入口时，前端必须给出明确可读提示，不允许静默失败。
3. 路由跳转过程若发生异常（如 `router.push` 失败），前端需回显错误提示，确保用户可感知失败原因。
4. 本策略属于 Windows 客户端交互层增强，不改变后端接口路径、参数、权限与错误码语义。

### 7.1.J Windows 订单小票化展示策略（本次新增）

1. 销售单结果区与下钻明细区按“小票化”展示最小字段集：商品名称、数量、单价、行小计。
2. 采购单结果区按“小票化”展示：商品名称、数量、单次进价、行小计，并展示订单总金额。
3. 若服务端返回商品名称兜底值（如 `商品#1001`），前端应原样展示，不在客户端二次拼接。
4. 该策略仅调整展示层，不改变创建/确认/作废/退货接口的请求结构。

### 7.1.K Windows 订单列表小票化展示策略（本次新增）

1. 销售单页面“订单下钻列表”采用“按订单卡片 + 明细表”的小票化渲染，不再仅显示订单摘要表格。
2. 每个订单卡片应包含：
   - 订单头：`biz_no`、状态、`total_amount`；
   - 明细区：`product_name / qty / sell_price / line_amount`；
   - 订单尾：`created_at / updated_at / remark`。
3. 对 `OUTBOUND_ONLY` 聚合行保留兼容展示：
   - `items=[]` 时不渲染明细表，但必须展示业务单号、订单总金额与备注。
4. 本策略属于 Presentation 层增强，不改变下钻查询参数、分页语义、权限与错误码语义。

### 7.1.L Windows 入库/出库明细展示增强策略（本次新增）

1. **出库明细展示增强**
   - 出库明细行需持有并展示 `product_name`，用于作业核对；
   - 扫码命中商品并写入明细时，同步写入 `product_name`；
   - 手工新增明细时允许补录商品名称，缺失时可按兜底值展示（如 `商品#<product_id>`）。
2. **版本字段收敛**
   - `expected_version` 从主作业输入项收敛为非主输入（可隐藏或放入高级区）；
   - 保持请求组包兼容，不改变 `POST /inventory/outbound` 契约。
3. **入库待提交明细增强**
   - 待提交明细必须展示 `product_name`；
   - `unit_cost` 支持行级编辑，编辑后作用于该行全部数量；
   - 同商品合并行为下，编辑的是合并行统一进价。
4. 本策略仅涉及 Windows 客户端编排与展示层，不新增后端接口，不变更权限、错误码、幂等语义。

### 7.1.M Windows 入库提交按钮下沉策略（本次新增）

1. Windows 入库页采用“两段式”动作分层：
   - 表单区仅保留“加入明细”（主）与“重置”（次）。
   - “提交入库”主动作迁移至“待提交入库明细”区域。
2. 提交按钮可用性约束：
   - 待提交明细为空时，提交按钮禁用；
   - 明细非空时允许提交，并沿用既有单条/批量编排策略。
3. 该改造仅作用于 Presentation 层，不改变 `submit/inboundBatchApi` 组包字段、不改变后端接口与错误码语义。

### 7.1.N Windows 入库手工录入区折叠与提交路径收敛（本次新增）

1. 入库页“手工录入区（商品ID/条码/数量/单价/版本/备注）”默认折叠展示，按需展开。
2. 手工录入区仅承担“加入明细”职责，不直接触发入库提交。
3. 入库提交入口统一位于“待提交入库明细”区，且必须存在至少 1 条有效明细才可提交。
4. 该策略属于 Windows 前端交互收敛，不新增后端接口、不改变错误码与权限语义。

### 7.1.O Windows 出库单据头折叠与高级字段分组（本次新增）

1. 出库页单据头录入区默认折叠，减少高频扫码场景视觉干扰。
2. `expected_version` 默认进入“高级字段”分组，按需展开。
3. 明细编辑、扫码写入、提交校验与请求组包保持现有语义，不改变 `POST /inventory/outbound` 契约。

### 7.1.A Windows 创建流程单号交互约束（本次新增）

1. 入库/出库/采购单/销售单/盘点单创建页不再提供 `biz_no` 可编辑输入框。
2. Windows 端创建请求组包不再包含 `biz_no` 字段。
3. 创建成功后在结果区展示服务端返回的 `data.biz_no`，用于核对、打印与审计追踪。
4. 该变更仅发生在 Windows 前端展示与组包层，不改变后端权限、幂等与错误码语义。

### 7.1.P Windows 视觉体系与微交互升级策略（本次新增）

1. **设计令牌统一化**
   - 在 `client/src/style.css` 建立统一设计令牌：主色、功能色、背景层级、圆角、阴影、间距、过渡时长。
   - 主色收敛为深夜蓝；功能色语义固定为入库绿、出库橙、库存不足红。

2. **卡片体系升级**
   - 页面卡片统一采用 `12~16px` 圆角与浅弥散阴影，弱化硬边框。
   - 通过浅品牌底色（低透明度）增强模块分区识别，避免大面积纯白平铺。

3. **信息层级与数据可视化**
   - 看板核心金额使用等宽数字字体与更大字号，提升财务读数稳定性。
   - 次要元信息（ID/单号/操作人）统一降级为小号中灰文本。
   - 增加轻量可视化：
     - 看板销售趋势 Sparkline（前端衍生数据）
     - 低库存进度条（库存与阈值关系）

4. **图标与交互一致性**
   - 侧边导航入口采用双色（Duotone）图标风格；
   - 列表操作采用线性图标按钮，保持线宽与圆角一致。

5. **动效与加载状态**
   - 查询态优先使用 Skeleton 骨架屏，替代单一文字加载态。
   - 看板金额支持 Count-up 动画，页面切换提供轻量过渡（淡入/位移）。

6. **边界与兼容**
   - 本次仅改 Windows 前端 `presentation` 层。
   - 不新增后端接口，不改变 API 参数、权限、错误码、幂等语义。

## 7.2 Android 端（Flutter）
- `MobileScanner` 连续扫码 + 300ms 防抖。
- `Isar` 本地缓存读取。
- 本地 `sync_queue` 写入离线操作，后台 Worker 重放同步。
- 每条写请求附带 `X-Idempotency-Key`，确保重放安全。

### 7.2.A Android UI/UX 分层与布局优化约束（本次新增）

1. **分层边界不变**
   - UI/UX 优化仅发生在 `presentation` 层（页面结构、视觉样式、交互反馈）。
   - `application/repository/network` 层保持既有契约，不因样式优化改变业务语义。

2. **页面布局分区规范**
   - 高频作业页统一采用“`状态提示区 -> 快捷操作区 -> 主表单区 -> 结果区`”的分区结构。
   - 看板页统一采用“`经营指标区 -> 高频入口区 -> 查询筛选区`”结构。
   - 登录页统一采用“单主卡片表单 + 明确反馈区”，避免控件散落。

3. **视觉一致性规范**
   - 使用统一主题色、圆角、间距与卡片样式，降低页面割裂感。
   - 主动作按钮（提交/查询/扫码）与次动作按钮（重置/取消）需视觉分级。
   - 错误与成功反馈采用统一样式组件，减少认知负担。

4. **演进原则**
   - 不新增后端接口，不修改字段与错误码定义。
   - 允许在 Flutter 端新增通用样式与页面组件以提升复用性。
   - 条码录入交互优先采用“输入框尾部扫码图标”触发摄像头扫码，避免页面出现冗余独立扫码按钮。

### 7.2.C Android 高配软装主题与组件化架构（本次新增）

1. **主题 Token 中心化**
   - 在 Flutter 端建立统一 Theme Token（颜色、圆角、阴影、间距、动效时长）。
   - 页面禁止直接散落硬编码主视觉参数，优先通过 Theme/通用组件消费。
   - 需同时提供 Light / Dark 主题，保证夜间与低光环境可用性。

2. **通用视觉组件分层**
   - 新增可复用基础视觉组件：品牌头图（Hero Banner）、分区卡片（Section Card）、状态提示（Status Notice）。
   - 页面通过组合组件构建“头图 -> 分区 -> 状态”结构，降低样式重复。

3. **页面骨架统一约束**
   - 登录页：品牌头图区 + 认证卡片区 + 状态反馈区。
   - 看板页：欢迎头图区 + 快捷入口区 + 查询区 + 指标区。
   - 作业页（商品/入库/出库/盘点）：头图区 + 扫码/筛选区 + 主表单区 + 结果区。

4. **兼容性与边界**
   - 本次升级仅限 `presentation` 层，不改 `application/repository/network` 的协议语义。
   - 不引入新的后端 API，不调整既有错误码处理流程。

### 7.2.0 Android 条码录入统一摄像头方案（本次新增）

1. **设计目标**
   - 所有“可手动输入条码”的输入位都可通过手机摄像头扫码回填。
   - 保留扫码枪/手输路径，摄像头扫码作为并行录入能力。

2. **架构约束**
   - 摄像头扫码仅影响前端交互层（Presentation 层），不改变服务端接口与字段契约。
   - 识别结果统一回填到现有 `barcode` 输入控件，并复用原业务动作（查询/累加/提交流程）。

3. **连续扫码会话模式（本次补充）**
   - 入库与出库页允许开启 `continuous_scan` 会话模式。
   - 打开摄像头后可持续识别多个条码，识别成功后继续下一条，不自动退出。
   - 每次识别成功提供页面提示 + 语音/听觉反馈。
   - 会话由用户点击“结束扫码”主动关闭。

4. **能力覆盖范围**
   - 入库：扫码入库输入位、入库表单 `barcode` 输入位。
   - 出库：扫码出库输入位。
   - 盘点：扫码选品输入位。
   - 商品：列表筛选、创建与编辑中的 `barcode` 输入位。

5. **异常处理约束**
   - 相机权限拒绝、用户取消、未识别到条码时，不中断页面原流程。
   - 保持现有错误码语义和角色权限逻辑（`4030/4040/4091` 等）。

### 7.2.B Android 创建流程单号交互约束（本次新增）

1. 入库/出库/盘点创建页面不再提供可编辑 `biz_no` 输入框。
2. 提交创建请求时，客户端不要求填写 `biz_no`。
3. 创建成功后在结果区展示服务端返回的 `biz_no`，用于追踪与核对。
4. 该优化仅调整前端交互，不改变幂等、权限、错误码语义。

### 7.2.D Android 入库未命中直达建档策略（本次新增）

1. 在 Android 入库页扫码流程中，若本地商品查询返回 `4040`，展示弹窗引导用户直达新建商品。
2. 仅 `OWNER/PURCHASER` 允许进入新建商品页；无权限角色仅显示提示，不展示跳转动作。
3. 跳转时将未命中条码透传给商品创建入口，自动回填条码输入框。
4. 新建成功后，回传已创建商品信息到入库页并自动回填入库表单，减少重复录入。
5. 该能力属于 Presentation/Application 层流程编排，不变更 API 契约。

### 7.2.E Android 扫码模式记忆与确认续扫策略（本次新增）

1. Android 入库/出库页需记忆用户上次选择的扫码模式，并在页面重入时自动恢复。
2. 模式偏好存储按“用户 + 终端 + 页面”隔离；退出登录时清理当前用户偏好。
3. `scan_confirm` 模式采用“确认后续扫”会话策略：
   - 扫码命中后弹确认区；
   - 点击确认后自动返回下一次扫码等待态；
   - 支持手动结束会话，并展示会话处理计数。
4. 同码短时去重与成功反馈（提示/声音）策略保持生效。
5. 本次仅改客户端交互与本地偏好存储，不新增后端接口。

### 7.2.F Android 扫码模式 Tab 化与智能触发策略（本次新增）

1. Android 入库/出库页扫码模式切换改为并排 Tab（如 `ChoiceChip/SegmentedButton`），双模式直观可切换。
2. 在 `scan_confirm` 模式下，点击主处理按钮时：
   - 若条码输入非空，按原流程处理；
   - 若条码输入为空，智能拉起摄像头扫码，识别成功后自动回到原流程。
3. 在 `continuous_scan` 模式下，主按钮行为保持“开启/维持连续扫码会话”，不做空输入智能分流。
4. 兼容迁移：若读到历史值 `quick_accumulate`，客户端应映射到 `continuous_scan` 并写回本地偏好。
5. 该增强不改变控制器请求组包、权限校验、错误码处理与后端 API 契约。

### 7.2.G Android 入库确认写入弹窗化策略（本次新增）

1. 仅在 Android 入库页 `scan_confirm` 模式中启用确认写入弹窗。
2. 扫码命中商品后弹出对话框承载确认输入（商品信息 + 数量），不再以内联区块确认。
3. 弹窗确认后沿用既有控制器写入流程，并自动进入下一次扫码等待态。
4. 弹窗需支持键盘回车快速确认（扫码枪/外接键盘场景），取消后不影响继续扫码。
5. 入库提交成功后重置策略与 Windows 一致：
   - 清空 `product_id`、`barcode` 与扫码输入条码；
   - 数量恢复默认值 `1`；
   - 维持其余字段清空策略，避免误提交上一笔上下文。
6. 本次仅调整 `presentation` 层交互，不改变 `application/repository/network` 契约与语义。

### 7.2.K Android 出库确认写入弹窗化策略（本次新增）

1. 仅在 Android 出库页 `scan_confirm` 模式下启用确认写入弹窗（`AlertDialog`）。
2. 扫码命中后通过弹窗承载确认字段（商品信息、数量、销售单价、可选版本），替代页面内联确认区。
3. 弹窗确认成功后自动回到扫码等待态，保持“确认后续扫”会话计数与聚焦策略。
4. 本次仅调整 Android `presentation` 层，不改变 `application/repository/network` 与后端 API 契约。

### 7.2.H Android 看板订单下钻点击反馈与联动策略（本次新增）

1. Android 端经营看板“今日订单数”指标卡改为可点击交互入口，点击后必须产生可感知结果（跳转成功、权限提示或错误提示）。
2. 角色约束：
   - `OWNER/SALES`：允许进入订单下钻页；
   - `PURCHASER`：不允许进入，点击后立即提示无权限（不得无反馈）。
3. 下钻页默认查询参数：
   - `start_date = dashboard.date`
   - `end_date = dashboard.date`
   - `page = 1`
   - `page_size = 10`（默认）。
   - 用户手动修改后，如输入为空或非法，客户端回落 `10`。
4. 下钻页支持用户修改日期范围并触发分页查询；所有查询继续复用既有接口 `GET /reports/dashboard/orders`，不新增后端 API。
5. 日期校验策略：
   - 日期格式必须为 `YYYY-MM-DD`；
   - `start_date <= end_date`；
   - 校验失败时在客户端即时提示，禁止发送无效请求。
6. 该策略属于 Android `presentation/application` 层增强，不改变后端权限、错误码、幂等与统计口径语义。

### 7.2.I Android 订单下钻列表小票化展示策略（本次新增）

1. Android 端 `DashboardOrdersPage` 的订单列表采用“每单一张小票卡片”渲染，不再仅使用摘要型 `ListTile`。
2. 小票卡片最小字段集：
   - 订单头：`biz_no`、`status`、`total_amount`；
   - 明细区：`product_name / qty / sell_price`（Android 展示层不显示 `line_amount`）；
   - 订单尾：`created_at / remark`（Android 展示层不显示 `updated_at`）。
3. 对聚合行（如 `status=OUTBOUND_ONLY`）必须保留订单级展示；若返回了聚合明细则按普通明细渲染，仅在 `items=[]` 时显示“无明细（聚合行）”提示。
4. 服务端响应中的 `line_amount` 与 `updated_at` 字段继续保留用于跨端兼容，Android 客户端可按展示策略忽略。
5. 该策略仅涉及 Android `presentation` 层展示增强，不改变 `GET /reports/dashboard/orders` 查询参数、返回结构、权限和错误码语义。

### 7.2.J Android 日期输入日历化策略（本次新增）

1. Android App 中所有需要填写日期的输入位统一改为 `showDatePicker` 日历选择，不再使用自由文本手输日期。
2. 默认值约束：
   - 看板单日期查询默认当天；
   - 订单下钻日期范围查询默认 `start_date=end_date=当天`。
3. 日期输入组件需支持“点击输入框”与“点击日历图标”两种触发方式，提升可用性。
4. 选中日期后由客户端统一格式化为 `YYYY-MM-DD` 再参与控制器校验与接口请求。
5. 当初始化参数为空或返回日期字段缺失时，客户端需自动回落为当天日期，避免空值状态进入查询流程。
6. 本策略仅作用于 Android `presentation/application` 层，不改变 `GET /reports/dashboard` 与 `GET /reports/dashboard/orders` 的接口契约、权限和错误码语义。

### 7.2.L Android 入库/出库明细展示增强策略（本次新增）

1. **Android 出库明细展示增强**
   - 出库明细模型需持有 `productName`，用于明细卡片展示；
   - 扫码确认写入时同步持久化 `productName`；
   - 版本字段 `expectedVersion` 收敛为非主输入，不作为默认高频编辑项。
2. **Android 入库待提交明细增强**
   - 待提交明细卡片必须展示 `productName`；
   - 每条明细需支持 `unitCost` 行级编辑；
   - 同商品合并后编辑 `unitCost` 作用于该合并行全部数量（统一修改）。
3. **校验与兼容边界**
   - `unitCost` 允许为空；为空时沿用服务端回落 `cost_price`；
   - 非空时仍需满足金额格式（最多 4 位小数）；
   - 本次仅改客户端 `presentation/application` 层，不新增后端接口，不改变权限、错误码、幂等语义。

### 7.2.M Android 采购记录入口与只读页策略（本次新增）

1. 在 `DashboardPage` 新增“采购记录”快捷入口，入口对 `OWNER/PURCHASER` 可见。
2. 新增采购记录页面（`PurchaseRecordsPage`）与控制器（`PurchaseRecordsController`），分层职责如下：
   - `models`：新增采购记录分页查询模型与 `PurchaseOrderData` 映射；
   - `repository`：新增 `fetchPurchaseOrders` 调用 `GET /purchase-orders`；
   - `application`：复用日期范围校验、分页状态管理与错误归一化模式；
   - `presentation`：提供日期范围、每页条数、分页按钮与只读小票卡片展示。
3. 首屏默认查询参数：`start_date=end_date=当天`、`page=1`、`page_size=10`，支持用户调整每页条数。
4. 页面只提供读取与筛选，不提供编辑/确认/作废等采购单写操作入口。
5. 应用装配层（`app.dart`）负责采购记录控制器生命周期管理，与登录会话绑定创建/释放。

### 7.2.O Android 入库记录入口与只读页策略（本次新增）

1. 在 `DashboardPage` 将“采购记录”快捷入口替换为“入库记录”，入口对 `OWNER/PURCHASER` 可见。
2. 复用库存模块分层，不新增后端接口：
   - `models`：新增库存流水分页查询模型（`StockLogData`）；
   - `repository`：新增 `fetchInboundLogs`，调用 `GET /inventory/logs` 并固定 `biz_type=IN_PURCHASE`；
   - `application`：新增 `InboundLogsController`，复用日期范围校验、分页状态与错误归一化；
   - `presentation`：入库记录页只读展示流水头与库存快照。
3. 首屏默认查询参数：`start_date=end_date=当天`、`page=1`、`page_size=10`。
4. 页面仅提供读取与筛选，不提供编辑、作废、回写库存等写入口。
5. 应用装配层（`app.dart`）负责入库记录控制器生命周期管理，并与登录会话绑定创建/释放。

### 7.2.N Android 入库提交按钮下沉策略（本次新增）

1. Android 入库页采用“两段式”交互：
   - 表单区主动作聚焦“加入明细”；
   - “提交入库”主按钮下沉到“待提交明细”卡片区域。
2. 提交按钮状态约束：
   - 待提交明细为空时禁用提交并提示“请先加入明细”；
   - 非空时允许提交，提交链路继续复用控制器既有单条/批量策略。
3. 本策略仅调整 `presentation` 层布局与交互，不新增后端 API，不改变权限、错误码与幂等语义。

### 7.2.P Android 销售趋势与毛利趋势分析页架构（本次新增）

1. **新增页面**
   - `SalesTrendPage`：销售趋势分析页，从“今日销售额”指标卡点击进入。
   - `ProfitTrendPage`：毛利趋势分析页，从“今日毛利润”指标卡点击进入。
   - “今日订单数”保持现有订单下钻列表页不变。

2. **分层职责**
   - `models` 层：新增 `TrendDayData` 模型，字段与 `DashboardData` 一致。
   - `repository` 层：新增 `fetchTrend(startDate, endDate)` 调用 `GET /reports/trend`。
   - `application` 层：新增 `TrendController`，管理趋势数据加载状态、错误处理。
   - `presentation` 层：使用 `fl_chart` 绘制折线图、柱状图、饼图。

3. **图表库依赖**
   - Flutter 端新增 `fl_chart` 依赖，用于绘制趋势图表。
   - 图表需支持加载动画、触摸节点查看数值。

4. **销售趋势页结构**
   - 顶部汇总卡片：今日销售额（大字） + 订单数。
   - 近 7 天销售额折线图（数据来源：`GET /reports/trend`）。
   - 当日商品构成饼图（数据来源：`GET /reports/dashboard/orders` 订单明细按商品聚合）。

5. **毛利趋势页结构**
   - 顶部汇总卡片：今日毛利（大字） + 毛利率。
   - 近 7 天毛利柱状图 + 销售额对比线（数据来源：`GET /reports/trend`）。
   - 底部毛利口径说明文案。

6. **权限控制**
   - 仅 `OWNER/SALES` 可进入两个分析页；
   - `PURCHASER` 点击时弹出拦截提示，不允许进入。

7. **生命周期管理**
   - `TrendController` 由 `app.dart` 统一管理，与登录会话绑定创建/释放。
   - `DashboardPage` 注入并使用 `TrendController`。

8. **兆容与边界**
   - 本次新增后端 `GET /reports/trend` 接口。
   - 其余不改变现有权限、错误码与幂等语义。

### 7.2.Q Android 低库存商品下钻页架构（本次新增）

1. **入口**：看板"低库存商品数"指标卡点击，所有角色（`OWNER/PURCHASER/SALES`）均可进入，无权限拦截。
2. **分层职责**
   - `repository` 层：复用现有 `ProductRepository.listProducts()`，新增 `lowStock: bool` 参数，调用 `GET /products?low_stock=true`。
   - `application` 层：新增 `LowStockController`（`ChangeNotifier`），管理加载状态、商品列表与错误处理。
   - `presentation` 层：新增 `LowStockPage`，只读展示商品名 + 当前库存 + 阈值 + 缺口 + 进度条。
3. **生命周期**：`LowStockController` 由 `app.dart` 统一管理，与登录会话绑定创建/释放。
4. **数据排序**：前端按 `min_stock_limit - current_stock` 降序展示（缺口最大的排最前）；也可依赖后端 `low_stock=true` 时的排序，两者结果一致。
5. **进度条计算**：`ratio = current_stock / min_stock_limit`，值域 `[0.0, 1.0)`，用 `LinearProgressIndicator` 展示，颜色随比例变化（红 < 0.3，橙 < 0.6，绿 >= 0.6）。
6. **空状态**：列表为空时展示友好提示，不展示空白页。
7. **约束**：无写操作入口，不改变后端接口权限与错误码语义。

### 7.2.1 Android 商品管理模块分层（本次新增）

按现有 Flutter 代码组织，商品管理模块建议与库存模块保持一致分层：

1. **models 层**
   - `product_models.dart`：
     - `ProductData`
     - `ListProductsQuery`
     - `CreateProductRequest`
     - `UpdateProductRequest`
     - `DeleteProductQuery`
     - `DeleteProductResult`
     - `PagedData<ProductData>`
   - 解析字段与服务端契约保持一致：
     - `id/sku/barcode/name/unit/current_stock`
     - `retail_price/wholesale_price/cost_price`
     - `min_stock_limit/version`

2. **repository 层**
   - `product_repository.dart`：
     - `listProducts()` -> `GET /products`
     - `createProduct()` -> `POST /products`
     - `updateProduct()` -> `PUT /products/{id}`
     - `deleteProduct()` -> `DELETE /products/{id}`
   - 复用统一 `ApiClient`，由拦截器自动注入 `x-request-id`、`x-idempotency-key`、`Authorization`。

3. **application 层**
   - `product_controller.dart`：
     - 查询条件状态（`keyword/barcode/page/page_size`）
     - 列表加载状态（`loading/list/total/error`）
     - 新建/编辑/删除提交状态（`submitting/success/error`）
   - 与 `InboundController/OutboundController` 保持同类错误归一化策略（含 `4091` 版本冲突提示）。

4. **presentation 层**
   - `products_page.dart`：
     - 列表+搜索区域（关键字/条码）
     - FAB 新建入口 + BottomSheet 新建表单
     - 卡片化商品列表与操作区（编辑/删除）
   - `products_edit_page.dart`（可独立文件或同模块独立路由页面）：
     - 全屏编辑表单（含 `expected_version`）
     - 摄像头扫码回填条码输入位
     - 长表单提交与错误提示稳定展示

5. **依赖注入与装配**
   - `AppServices` 注入 `ProductRepository`。
   - `JxcApp` 在会话恢复后创建并托管 `ProductController` 生命周期。
   - `DashboardPage` 注入并使用 `ProductController`，提供“商品管理”快捷入口。

### 7.2.2 Android 商品管理权限与数据可见性

1. **角色能力模型（前后端一致）**
   - `OWNER/PURCHASER`：可读写（创建/更新/删除）。
   - `SALES`：仅只读（列表、详情），不可调用写操作。

2. **敏感字段可见性**
   - `SALES` 角色 `cost_price` 固定隐藏（服务端返回 `null` + 前端不展示列）。

3. **页面权限防线**
   - UI 层：无写权限时隐藏或禁用“新建/保存/删除”。
   - Controller 层：提交前二次校验角色，阻断误调用。
   - API 层：服务端 `ensure_role` 兜底（`4030`）。

4. **并发与一致性**
   - 更新/删除携带 `expected_version`。
   - 服务端返回 `4091` 时，前端提示“刷新后重试”并触发列表刷新。

---

## 8. 安全、可观测与运维

## 8.1 安全
- JWT Access Token（2小时）+ Refresh Token（7天）。
- 关键管理操作记录审计日志。
- 密码加盐哈希存储（Argon2 或 bcrypt）。

## 8.2 可观测
- 每个请求生成并返回 `request_id`。
- 核心监控：接口延迟P95、错误率、库存冲突率、离线同步成功率。

## 8.3 运维与发布
- PostgreSQL 开启 WAL，凌晨全量备份到对象存储。
- 后端 Docker 多阶段构建。
- CI/CD 自动构建 Windows 客户端与 Android APK。

## 8.4 审计增强
- 高风险操作必须记录 `before_data/after_data`：
  - 改价、改单、作废、权限变更、库存阈值修改。
- 审计日志必须携带 `request_id` 与操作者信息，满足问题追溯。

---

## 9. 风险与技术决策

1. **并发超卖风险**：通过事务 + 行级锁 + 库存校验规避。
2. **离线重复提交风险**：通过幂等键与幂等记录规避。
3. **跨文档语义漂移风险**：建立字段字典与错误码字典统一维护。

---

## 10. 商品批次管理架构（v1.4.0 新增）

### 10.1 数据库层

新增表 `product_batches`，与现有表关系：

```
products (id, tenant_id, ...)
  └── product_batches (id, tenant_id, product_id, ...)
```

**表结构**

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | `BIGINT` | `PK` | 主键 |
| `tenant_id` | `UUID` | `NOT NULL` | 租户隔离 |
| `product_id` | `BIGINT` | `NOT NULL FK` | 关联商品 |
| `lot_number` | `TEXT` | 可空 | 批次号 |
| `inbound_at` | `DATE` | `NOT NULL` | 入库日期 |
| `produced_at` | `DATE` | 可空 | 生产日期 |
| `expires_at` | `DATE` | 可空 | 过期日期 |
| `is_sold_out` | `BOOLEAN` | `DEFAULT false` | 售完标记 |
| `notes` | `TEXT` | 可空 | 备注 |
| `created_at` | `TIMESTAMPTZ` | `NOT NULL DEFAULT NOW()` | 创建时间 |

**`products` 表新增字次（v1.4.1）**

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `track_batches` | `BOOLEAN` | `DEFAULT false` | 是否启用批次追踪；入库响应返回该标记供客户端决策是否弹窗 |

**migration 要求**：对现有商品表执行 `ALTER TABLE products ADD COLUMN track_batches BOOLEAN NOT NULL DEFAULT false`，无需回写历史数据。

**索引策略**
- `(tenant_id, product_id)` 复合索引 — 支持按商品分页查询
- `(tenant_id, expires_at, is_sold_out)` 复合索引 — 首页临期预警查询快速路径

### 10.2 服务端分层

```
Routes
  └── /products/{id}/batches          GET/POST
  └── /products/{id}/batches/{bid}    PUT/DELETE
  └── /products/{id}/batches/{bid}/sold-out  POST
  └── /products/batches/expiring      GET

Repository
  └── product_batches
        ├── list_batches(tenant_id, product_id, only_active) -> Vec<BatchData>
        ├── create_batch(tenant_id, product_id, req) -> BatchData
        ├── get_batch(tenant_id, id) -> Option<BatchData>
        ├── update_batch(tenant_id, id, req) -> BatchData
        ├── mark_sold_out(tenant_id, id) -> BatchData
        ├── delete_batch(tenant_id, id) -> ()
        └── list_expiring(tenant_id, days) -> Vec<ExpiringBatchData>
```

### 10.3 效期等级计算

效期等级在服务端计算，不依赖客户端时间，返回给前端作展示与排序依据：

```
dfs = (expires_at - today).days

expiry_level =
  | dfs < 0     -> expired   (灰色)
  | dfs < 7     -> critical  (红色)
  | dfs < 15    -> warning   (橙色)
  | dfs < 30    -> notice    (黄色)
  | dfs >= 30   -> ok        (默认)
  | no expires_at -> null
```

### 10.4 Flutter 分层

```
features/products/
  ├── models/
  │   └── batch_models.dart          BatchData, ExpiringBatchData,
  │                                  CreateBatchRequest, UpdateBatchRequest
  ├── data/
  │   └── batch_repository.dart      HTTP 层：create/list/update/markSoldOut/delete/expiring
  ├── application/
  │   └── batch_controller.dart      ChangeNotifier：batches/loading/submitting/error
  └── presentation/
      ├── batch_management_page.dart  批次列表页：按商品展示/编辑/标记售完/删除
      └── create_batch_sheet.dart     新建批次 BottomSheet
```

**生命周期管理**
- `BatchController` 由 `app.dart` 统一创建与销毁，与登录会话绑定。
- `DashboardPage` 与 `ProductsPage` 均直接使用该 controller。

### 10.5 首页效期监控卡片

`DashboardPage` 的「批次效期监控」卡片常驻展示，根据临期批次数据动态渲染：

| 状态 | 展示逻辑 |
|---|---|
| 加载中 | 展示 骨架屏，避免闪烁 |
| 有临期批次 | 展示红/橙/黄色预警列表，按天数升序排列 |
| 无临期批次 | 绿色卡片：“✅ 暂无临期批次” |

`AnimatedBuilder` 监听 `BatchController`，当控制器状态变化时自动重建。

---

## 11. Android UI 两个重要两个互交优化架构约束（v1.4.0 新增）

### 11.1 刷新按鈕清理

移除各列表页嵌入在 AppBar 中的刷新 `IconButton`，改由内置 `RefreshIndicator`（下拉刺新）触发。

**受影响页面**：`Products/BatchManagement/LowStock/InboundLogs/StockCheckLogs/Users`。

**首页保留**：`DashboardPage` 的刷新按鈕保留，因为该页实时性强。

### 11.2 新建商品入口位置

- `ProductsPage` 移除 `FloatingActionButton`，将「新建商品」迁移到 AppBar `actions` 区 `IconButton(Icons.add_rounded)`。
- `UsersPage` 移除 `FloatingActionButton`，将「添加员工」迁移到 AppBar `actions` 区 `IconButton(Icons.person_add_rounded)`。
- 两处变更同时调整 `ListView` 底部 `padding`，从博动按鈕预留空间（96px）恢复为普通内边距（24px）。

### 11.3 日历中文化

- `pubspec.yaml` 新增依赖：`flutter_localizations`。
- `app.dart` 项目 MaterialApp 新增：
  - `locale: const Locale('zh', 'CN')`
  - `localizationsDelegates: [GlobalMaterialLocalizations.delegate, GlobalWidgetsLocalizations.delegate, GlobalCupertinoLocalizations.delegate]`
  - `supportedLocales: [Locale('zh', 'CN'), Locale('en', 'US')]`
- 不改变接口参数，日期仍为 `YYYY-MM-DD` 字符串传输。

### 11.4 首页智能自动刷新架构约束

1. `_DashboardPageState` 混入 `WidgetsBindingObserver`，绑定生命周期：
   - `initState`：调用 `WidgetsBinding.instance.addObserver(this)`
   - `dispose`：调用 `WidgetsBinding.instance.removeObserver(this)`
   - `didChangeAppLifecycleState(resumed)`：触发防抖刷新 `_autoRefresh()`

2. **刷新分两种路径**

   | 路径 | 方法 | 触发时机 |
   |---|---|---|
   | 强制刷新 | `_load()` | 入库/出库返回，必然有数据变化 |
   | 防抖刷新 | `_autoRefresh()` | App 前台切回、盘点返回、低库存返回 |

3. **防抖实现**：维护 `_lastAutoRefresh: DateTime?`，30 秒内重复调用 `_autoRefresh()` 直接 return，不发出网络请求。

4. **导航改造**：`_handleQuickActionTap` 从 `void` 改为 `Future<void>`，各操作页导航均改为 `await Navigator.push`，返回后根据操作类型执行对应刷新策略。

5. **不引入轮询 Timer**：本方案零定时器，刷新由用户行为（切回前台 / 操作完成返回）驱动，不产生静默后台请求。

6. **API 层不变**：所有刷新均调用现有 `DashboardController.load()` 与 `BatchController.loadExpiring()`，无新增接口，无权限与错误码变更。

---

## 12. 入库批次关联弹窗架构（v1.4.1 新增）

### 12.1 服务端改动

**`products` 表**：新增 `track_batches BOOLEAN NOT NULL DEFAULT false`，由创建/编辑商品接口维护。

**入库接口响应扩展**：
- `POST /inventory/inbound` → `data` 新增 `track_batches: bool`
- `POST /inventory/inbound/batch` → `data.items[*]` 各自新增 `track_batches: bool`

服务端从 `products` 表读取关联商品的 `track_batches` 值写入响应，**不自动创建批次**，批次创建/关联由客户端发起。

### 12.2 Flutter 分层

```
InboundController（application 层）
  └── submitInbound() / submitBatchInbound()
        ↓ 成功后
        返回 InboundResult（含 trackBatchesProductIds: List<int>）
        ↓
InboundPage（presentation 层）
  └── 检查 trackBatchesProductIds 是否非空
        ↓ 非空
        依次对每个 product_id 调用 _showBatchLinkSheet(productId, productName)
        ↓
BatchLinkSheet（新建组件）
  ├── Tab 1: 选择已有批次
  │     └── GET /products/{id}/batches?only_active=true
  ├── Tab 2: 新建批次
  │     └── POST /products/{id}/batches
  └── 跳过按钮
```

### 12.3 新增文件

| 文件 | 职责 |
|---|---|
| `batch_link_sheet.dart` | 批次关联底部弹窗（`presentation` 层），含三 Tab：选已有/新建/跳过 |

### 12.4 `InboundController` 返回值约定

```dart
class InboundSubmitResult {
  final String bizNo;
  // product_id 列表：需要触发批次关联弹窗的商品
  final List<(int productId, String productName)> trackBatchesProducts;
}
```

- 单条入库：若响应 `track_batches = true`，则 `trackBatchesProducts = [(product_id, product_name)]`
- 批量入库：筛选 `items` 中 `track_batches = true` 的条目，去重后组成列表

### 12.5 弹窗顺序控制

入库页通过 `for` 循环 + `await` 串行展示弹窗，保证一次只弹一个：

```dart
for (final product in result.trackBatchesProducts) {
  await showModalBottomSheet(
    context: context,
    isDismissible: false,
    builder: (_) => BatchLinkSheet(productId: product.$1, productName: product.$2),
  );
}
```

### 12.6 商品编辑页变更

`ProductCreateSheet` / `ProductEditPage` 新增 `track_batches` 开关控件：
- 控件类型：`SwitchListTile`
- 默认值：`false`
- 保存时通过现有 `createProduct` / `updateProduct` 接口传递，无需新接口

### 12.7 兼容与边界

1. 旧版客户端不传 `track_batches`，服务端默认 `false`，行为不变。
2. 此特性仅影响入库后的客户端交互，不改变库存数量计算、成本计算、幂等语义。
3. `BatchController` 和 `BatchRepository` 复用现有实现，`BatchLinkSheet` 直接调用现有接口。
